# BaanLedger — Spec สำหรับส่งต่อ Claude Code

> เอกสารนี้เป็น **build spec** ไม่ใช่ marketing doc
> ทุกตัวเลขในเอกสารระบุ provenance: `[MEASURED]` = คำนวณเองในเอกสารนี้ / `[SOURCE]` = มีแหล่งอ้างอิง / `[ASSUME]` = สมมติ ต้องยืนยันก่อนใช้
> สิ่งที่ยังไม่ยืนยัน จะขึ้นต้นด้วย **⚠️ ต้องยืนยัน**

---

## 0. ขอบเขต

แอพ 2 โหมด ใช้ engine คำนวณตัวเดียวกัน:

| โหมด | ใช้ตอนไหน | ผลลัพธ์ |
|---|---|---|
| **Compare** | ก่อนเซ็นสัญญา | เปรียบเทียบข้อเสนอหลายธนาคารบนฐานที่ยุติธรรม → จัดอันดับ |
| **Track** | หลังเซ็นสัญญา | บันทึกการจ่ายจริง แยกต้น/ดอก กระทบยอดกับ statement ธนาคาร วางแผนโปะ |

**หลักการที่ทั้งแอพยึด:** เลขที่แสดงต้องตรงกับใบเสร็จธนาคารระดับสตางค์ ถ้าไม่ตรง ต้องบอกว่าต่างเท่าไหร่และสงสัยว่าเพราะอะไร — ไม่ปัดตกเงียบ ๆ

### 0.1 อยู่นอกขอบเขต v1 — ตัดสินใจแล้ว ห้ามแอบทำ

| ไม่ทำ | เหตุผล | ถ้าผู้ใช้เจอเคสนี้ |
|---|---|---|
| **เบิกเงินกู้หลายงวด** (ปลูกบ้านบนที่ดินตัวเอง / เบิกตามงวดงาน) | ต้องคิดดอกเบี้ยเฉพาะยอดที่เบิกแล้ว ซึ่งต้องมีตาราง `disbursements` และ engine ที่รับ principal เป็น step function — เปลี่ยนโครงสร้างแกนกลาง | ให้ import แบบ **Quick** (ข้อ 2A.5) โดยใช้ยอดหนี้ ณ วันที่เบิกครบแล้วเป็นจุดตั้งต้น และขึ้น banner ว่าประวัติช่วงก่อสร้างจะไม่มี |
| **ช่วงผ่อนดอกอย่างเดียว** (grace period) | มากับการเบิกหลายงวดเป็นส่วนใหญ่ | เหมือนข้างบน |

`active_loans.disbursed_amount_satang` จึงเป็น **ค่าเดียว** ได้ — เป็นการตัดสินใจ ไม่ใช่ข้อจำกัดที่มองข้าม

---

## 1. Domain Logic — ส่วนที่เครื่องคิดเลขทั่วไปทำผิด

### 1.1 ดอกเบี้ยคิดรายวัน ไม่ใช่รายเดือน

สินเชื่อบ้านไทยเป็นแบบลดต้นลดดอก (Effective Rate) คิดดอกเบี้ยรายวันจากเงินต้นคงเหลือ [SOURCE: ธนาคารอาคารสงเคราะห์, thaiinterest.com — สูตร `(เงินต้นคงเหลือ × อัตราต่อปี) / 365 = ดอกเบี้ยรายวัน`]

```
interest_accrued(d0 → d1) = Σ  balance(d) × rate(d) / 365
                          d ∈ [d0, d1)
```

**ห้ามใช้ `balance × rate / 12`** ผลต่างไม่ใช่เรื่องปัดเศษ:

| วิธี | เงินต้น 3,000,000 @ 3.00% เดือน 31 วัน |
|---|---|
| รายวัน /365 (ถูก) | 7,643.84 บาท |
| รายเดือน /12 (ผิด) | 7,500.00 บาท |
| ต่างกัน | **+143.84 บาท ในเดือนเดียว** |

`[MEASURED: 3,000,000 × 0.03 ÷ 365 × 31 = 7,643.8356]`

ผลลัพธ์: engine ต้องเป็น **daily event loop** ไม่ใช่ monthly amortization loop แบบ closed-form

### 1.1.1 ฐาน 365 หรือ 366 — สรุปการตัดสินใจ

**คำตอบ: ห้าม hardcode ทั้งสองแบบ ต้องเป็น config ระดับสัญญา และต้องเปลี่ยนตามวันที่มีผลได้**

หลักฐานว่าไทยไม่มีมาตรฐานเดียว:

| แหล่ง | สิ่งที่ระบุ |
|---|---|
| LH Bank (ประกาศผลิตภัณฑ์) | คิดรายวัน "ให้ถือว่าหนึ่งปีมี 365 วัน **หรือ 366 วัน ขึ้นอยู่กับปีนั้นๆ**" → ACT/ACT [SOURCE] |
| ttb, ธอส., thaiinterest | ตัวอย่างคำนวณใช้ `/365` [SOURCE] |
| แนวทางอ้างอิง ธปท. | จำนวนวันใน 1 ปี **ขึ้นกับการกำหนดของแต่ละสถาบันการเงิน** แต่ต้องใช้เลขเดียวกันทั้งดอกเบี้ยจ่าย (เงินฝาก) และดอกเบี้ยรับ (สินเชื่อ) [SOURCE: sanook อ้างแนวทาง ธปท.] |
| Pantip #42843584 | ผู้ใช้ track Excel เป๊ะทุกทศนิยม รายงานว่า **ธนาคารเดิมเปลี่ยนจาก /366 (ปีอธิกฯ ก่อนหน้า) มาเป็น /365** → convention เปลี่ยนกลางสัญญาได้ |
| Pantip #34865297 | ธนาคารหนึ่งหาร 365 **และคิดดอกวันที่ 29 ก.พ. ด้วย** → คือ ACT/365 Fixed (ปีนั้นจ่าย 366/365 ของอัตรา nominal) |

จึงต้องมี **3 โหมด** ไม่ใช่ 2:

```ts
type DayCountBasis =
  | 'ACT/365F'      // หาร 365 เสมอ + นับ 29 ก.พ. → ปีอธิกฯ จ่าย 366 วัน  ← DEFAULT
  | 'ACT/ACT'       // หาร 366 ในปีอธิกฯ → จ่ายพอดี 1 ปี
  | 'ACT/365_SKIP'  // หาร 365 + ไม่นับ 29 ก.พ.  ⚠️ ยังไม่พบหลักฐานว่าธนาคารไทยใช้จริง
                    //    ใส่ไว้เพื่อให้ inference engine ลองได้เท่านั้น
```

### 1.1.2 ผลกระทบเชิงตัวเลข — เพื่อให้รู้ว่าควรใส่ใจแค่ไหน

`[MEASURED]` ทุกตัวเลขในตารางนี้จาก simulation เงินกู้ 3,000,000 เริ่ม 1 ต.ค. 2026 daily engine

**ผลรวมตลอดอายุสัญญา**

| สถานการณ์ | ACT/365F | ACT/ACT | ต่างกัน |
|---|---|---|---|
| คงที่ 5.00% ค่างวด 16,200 | 2,758,560.20 | 2,753,376.69 | **5,183.51** (0.19%) |
| โปร 3 ปี 3.00% → 5.50% ค่างวด 17,500 | 2,080,750.82 | 2,077,503.98 | **3,246.84** (0.16%) |

**ผลกระทบรายงวด (ก.พ. 2028 = ปีอธิกฯ, เงินต้น 2,900,000 @5.00%, 29 วัน)**

| โหมด | ดอกเบี้ยงวดนั้น |
|---|---|
| ACT/365F | 11,520.55 |
| ACT/ACT | 11,489.07 |
| ACT/365_SKIP | 11,123.29 |

**ข้อสังเกตที่กำหนดสถาปัตยกรรม:**

1. **ในปีปกติ ACT/365F กับ ACT/ACT ให้ผลเหมือนกันทุกประการ** (ตัวหารเป็น 365 ทั้งคู่) → แยกความต่างไม่ได้เลยถ้าไม่มีข้อมูลจากปีอธิกสุรทิน ปีอธิกฯ ถัดไปคือ **2028**
2. ในปีอธิกฯ ผลต่างคือ **1 วันของดอกเบี้ยพอดี กระจายทั้งปี** ไม่ใช่กระจุกที่เดือน ก.พ. → เดือนละ ~31 บาทต่อเนื่อง 12 เดือน ตรวจจับได้จาก drift สะสม ไม่ใช่จากงวดเดียว
3. 31 บาท/เดือน ตกอยู่ในโซน **เหลือง** ของ threshold ในข้อ 3.3 พอดี → ถ้าเจอเหลืองต่อเนื่องทั้งปีอธิกฯ นั่นคือสัญญาณว่า basis ตั้งผิด ต้องมี rule ตรวจ pattern นี้โดยเฉพาะ ไม่ใช่แค่ดูทีละงวด
4. ACT/365_SKIP ต่างจาก ACT/365F ถึง ~397 บาทในงวด ก.พ. ปีอธิกฯ → โซนแดง แยกออกง่าย

**สรุปเป็นข้อปฏิบัติ:**

| โหมด | ต้องใส่ใจแค่ไหน | เหตุผล |
|---|---|---|
| **Compare** | แทบไม่สำคัญ | ต่างกัน 0.16–0.19% ของดอกเบี้ยรวม เทียบกับความไม่แน่นอนของ MRR ±1% ที่คิดเป็นหลักแสน → **ใช้ ACT/365F เหมือนกันทุกธนาคาร** สิ่งเดียวที่ห้ามทำคือใช้ basis ต่างกันระหว่างธนาคาร เพราะจะสร้างความต่างปลอม |
| **Track** | สำคัญมาก | เป้าหมายคือตรงระดับสตางค์ ถ้า basis ผิดจะเห็น drift ตลอดปีอธิกฯ |

**Schema:** `day_count_basis` ต้องเป็น **effective-dated** ไม่ใช่ field เดียวในตาราง loan เพราะมีหลักฐานว่าธนาคารเปลี่ยน convention กลางสัญญา

```sql
loan_conventions (
  id, loan_id, effective_from date,
  day_count_basis text,        -- 'ACT/365F' | 'ACT/ACT' | 'ACT/365_SKIP'
  interest_rounding text,
  source text,                 -- 'contract' | 'inferred' | 'user_override'
  confidence text,             -- 'confirmed' | 'assumed'
  note text
)
```

ตอนสร้างสัญญาใหม่ให้ตั้ง `ACT/365F` + `confidence='assumed'` และขึ้น banner ว่า
> ยังไม่ได้ยืนยันวิธีนับวันของธนาคาร จะยืนยันได้หลังมีใบแจ้งยอดของปี 2028

**Implementation note:** ตอน accrue ข้ามขอบปี (เช่น 15 ธ.ค. 2027 → 15 ม.ค. 2028) โหมด ACT/ACT ต้อง **ตัดช่วงที่ 31 ธ.ค.** แล้วใช้ตัวหารคนละตัวสองท่อน ห้ามใช้ตัวหารของปีที่งวดครบกำหนด

### 1.2 ลำดับการตัดชำระ

```
payment → ค่าธรรมเนียม/เบี้ยปรับค้าง (ถ้ามี)
        → ดอกเบี้ยค้างรับสะสม ณ วันจ่าย
        → ส่วนที่เหลือทั้งหมด ตัดเงินต้น
```

**Guard ที่ต้องมี:** ถ้า `payment < ดอกเบี้ยค้างรับ` → หนี้รวมเพิ่มขึ้น (negative amortization) ต้องขึ้น warning สีแดงในตาราง ไม่ใช่แค่แสดงเลขติดลบ

#### ✅ ตัดสินใจแล้ว: ส่วนที่จ่ายไม่พอไปอยู่ไหน — เป็น config 2 โหมด

ไม่มีหลักฐานว่าธนาคารไทยทำเหมือนกันทุกแห่ง จึงทำเป็น config ระดับสัญญา **ต้อง implement ทั้งสองโหมด** ไม่ใช่เลือกทางเดียวแล้วเติมทีหลัง

```ts
capitalise_unpaid_interest: boolean   // default false
```

| โหมด | เงินที่ขาดไปอยู่ไหน | เดือนถัดไปคิดดอกจากอะไร |
|---|---|---|
| `false` **แยกบัญชี ไม่ทบ** ← **default** | บัญชี `accrued_interest` ต่างหาก | เงินต้นเดิม ดอกค้างไม่งอกดอก |
| `true` **ทบต้น** | รวมเข้าเงินต้น | เงินต้นที่โตขึ้นแล้ว = คิดดอกจากดอก |

`[MEASURED]` P=3,000,000 @7.00% ค่างวด 17,000 (ดอกงวดแรก 17,835.62 → ขาด 835.62):

| | เงินต้น | ดอกค้าง | รวมหนี้ |
|---|---|---|---|
| `false` เดือน 12 | 2,999,945.21 | 6,051.58 | **3,005,996.78** |
| `true` เดือน 12 | 3,006,163.78 | 0.00 | **3,006,163.78** |

ต่าง 167 บาทในปีแรก และถ่างขึ้นเรื่อย ๆ

**ทำไม default เป็น `false`:** ป.พ.พ. ม.655 ห้ามคิดดอกเบี้ยทบต้น เว้นแต่ค้างชำระไม่น้อยกว่า 1 ปีและตกลงกันเป็นหนังสือ ธนาคารไทยจึงแสดง "ดอกเบี้ยค้างชำระ" แยกบรรทัดในใบแจ้งยอด
⚠️ ยังไม่ยืนยันจากใบแจ้งยอดจริง → **โยนเข้า `inferBankConventions` ด้วย** ให้แอพค้นพบเองเหมือน day-count basis

**ผลต่อส่วนอื่นของ spec:**
- `ScheduleRow` เพิ่ม `accruedInterestCarriedSatang`
- `payment_allocations` เพิ่ม `accrued_interest_after_satang`
- `loan_conventions` เพิ่มคอลัมน์ `capitalise_unpaid_interest`
- **TV-3 ต้องทดสอบทั้งสองโหมด** ไม่ใช่โหมดเดียว

### 1.3 โครงสร้างอัตราดอกเบี้ย — แยก 2 ชั้น

อย่าเก็บ "ดอกเบี้ยปีที่ 1 = 2.5%" เป็นเลขเดียว เพราะพอถึงช่วงลอยตัวจะแก้ไม่ได้ ต้องแยก:

**ชั้นที่ 1 — สัญญา (คงที่ตลอดอายุสัญญา)**
```ts
type RateStep = {
  from_month: number          // 1-indexed
  to_month: number | null     // null = จนจบสัญญา
  kind: 'fixed' | 'index_minus' | 'index_plus'
  fixed_rate_bps?: number     // ใช้เมื่อ kind='fixed' เช่น 250 = 2.50%
  index_code?: 'MRR' | 'MLR' | 'MOR'
  spread_bps?: number         // เช่น 175 = MRR - 1.75%
}
```

**ชั้นที่ 2 — Reference rate (เปลี่ยนตามประกาศธนาคาร, เก็บเป็น time series)**
```ts
type ReferenceRate = {
  bank_code: string
  index_code: 'MRR' | 'MLR' | 'MOR'
  rate_bps: number
  effective_date: string      // ISO date
  source_url?: string
  entered_at: string
}
```

อัตราจริง ณ วันใด ๆ = resolve จาก RateStep + ReferenceRate ที่ effective ณ วันนั้น

**⚠️ ต้องยืนยัน:** ไม่มี public API ของ MRR รายธนาคาร การ scrape เว็บธนาคารเปราะและเสี่ยงผิด ToS → **ให้กรอกมือ** พร้อมช่อง `source_url` + แสดง badge "อัปเดตล่าสุด N วันที่แล้ว" ถ้าเกิน 90 วันให้เตือน

### 1.4 ค่างวด (Installment)

**⚠️ ต้องยืนยันจากใบเสนอจริง:** ธนาคารมัก fix ค่างวดไว้ค่าเดียวตลอดสัญญา คำนวณจากอัตราลอยตัว ไม่ใช่อัตราโปรโมชั่น (ทำให้ช่วงโปรตัดเงินต้นได้เยอะ) แต่บางที่ไม่เป็นแบบนี้

รองรับ 2 โหมด:
```ts
installment_basis: 'quoted'              // กรอกค่างวดจากใบเสนอตรง ๆ  ← default
                 | 'computed_from_rate'  // คำนวณจาก PMT ที่อัตราที่ระบุ
```

สูตร PMT (**ใช้เฉพาะตอนประมาณค่างวดตั้งต้น ห้ามใช้สร้างตารางผ่อน**):
```
PMT = P × i / (1 − (1+i)^−n)   ,  i = อัตราต่อปี / 12
```
ตัวอย่าง: P=3,000,000 n=360 @6.00% → **≈ 17,987 บาท/เดือน** `[MEASURED]`
ตารางผ่อนจริงต้องมาจาก daily engine เสมอ ตัวเลขจะไม่ตรงกับ PMT เป๊ะ นี่คือเรื่องปกติ

### 1.4.1 กฎสร้างวันครบกำหนด

**(ก) กฎสิ้นเดือน**
`due_day_of_month = 31` พอเจอ ก.พ. ต้อง clamp เป็นวันสุดท้ายของเดือน แล้ว**เดือนถัดไปกลับไปใช้ 31** (ห้าม drift ลงเรื่อย ๆ)

> ตัวเลข `[MEASURED]` ทุกตัวในเอกสารนี้คำนวณด้วยกฎนี้ + accrual window `[งวดก่อนหน้า, งวดนี้)` + `rounding: 'none'` + `date_roll: 'none'` — ยืนยันแล้วว่า reproduce ได้ครบ

**(ข) ✅ ตัดสินใจแล้ว: วันครบกำหนดตรงวันหยุด — เป็น config 3 โหมด**

```ts
date_roll: 'none'       // ตัดตามวันในสัญญาเสมอ ไม่สนวันหยุด        ← default
         | 'preceding'  // ขยับมา "เร็วขึ้น" เป็นวันทำการก่อนหน้า
         | 'following'  // เลื่อนออกไปเป็นวันทำการถัดไป
```

**`preceding` มีอยู่จริง ไม่ใช่เคสสมมติ** `[SOURCE: ผู้ใช้ — สัญญาที่ถืออยู่จริง]`
สัญญาตัวอย่าง: ตัดทุกวันที่ 5 ถ้าวันที่ 5 ตรงเสาร์/อาทิตย์หรือวันหยุดธนาคาร **จะขยับมาเร็วขึ้น** เช่นตัดวันที่ 3 แทน
ทิศทางนี้ตรงข้ามกับสมมติฐานปกติ ถ้า engine เดาว่าเลื่อนออกจะผิดทิศทั้งหมด

`[MEASURED]` P=3,000,000 @5.00% ค่างวด 16,200 ตัดทุกวันที่ 5 เริ่ม 2026-10-05:

| `date_roll` | งวดที่ขยับ | ขยับสูงสุด | ดอกเบี้ยรวมตลอดสัญญา | ต่างจาก `none` |
|---|---|---|---|---|
| `none` | 0 / 356 | — | 2,758,560.20 | — |
| `preceding` | **122 / 356 (34%)** | 3 วัน | 2,757,632.62 | **−927.58** |
| `following` | 122 / 356 | 3 วัน | 2,759,468.07 | +907.87 |

ยอดรวมต่างไม่มาก แต่ **รายงวดคือคนละเรื่อง**:

| ขยับ | ผลต่างของงวดนั้น @ เงินต้น 3,000,000 |
|---|---|
| 1 วัน | 410.96 |
| 2 วัน | 821.92 |
| 3 วัน | 1,232.88 |

ทั้งหมดอยู่ใน**โซนแดง** (>50 บาท) ตามข้อ 3.3 → ถ้าตั้ง `date_roll` ผิด จะเห็นแดง 34% ของงวดตลอดชีวิตแอพ

**⚠️ ต้องมีปฏิทินวันหยุดธนาคาร** — `preceding`/`following` ทำงานไม่ได้ถ้ารู้แค่เสาร์-อาทิตย์
เทียบกัน: เสาร์-อาทิตย์อย่างเดียวขยับ 102 งวด / รวมวันหยุดธนาคารขยับ 122 งวด → **ต่างกัน 20 งวด** ที่จะกลายเป็นแดงถ้าปฏิทินไม่ครบ

```sql
bank_holidays (
  holiday_date date pk,
  name_th text,
  source text,          -- 'bot_announcement' | 'user_added'
  year int
)
```

ข้อจำกัดที่ต้องรู้: ธปท. ประกาศวันหยุดธนาคารปีต่อปี และมีวันหยุดที่**ไม่ตายตัว** (วันพระใหญ่ตามจันทรคติ วันหยุดชดเชย วันหยุดพิเศษที่ ครม. ประกาศกะทันหัน) จึง**คำนวณล่วงหน้า 30 ปีไม่ได้**

**วิธีรับมือ:**
- Seed ปฏิทินเท่าที่ประกาศแล้ว (ปีปัจจุบัน + ปีหน้า) ให้ผู้ใช้เพิ่มเองได้
- ปีที่ยังไม่มีข้อมูล → fallback เป็นเสาร์-อาทิตย์อย่างเดียว และ **ติดป้ายในตารางผ่อนว่าเป็นวันประมาณการ**
- **Track mode ไม่ได้รับผลกระทบ** เพราะผู้ใช้บันทึกวันที่จ่ายจริง daily engine ใช้วันนั้นตรง ๆ อยู่แล้ว — ปัญหานี้กระทบเฉพาะ **ตารางที่ projection ไปข้างหน้า**
- โยน `date_roll` เข้า `inferBankConventions` ให้แอพค้นพบเองจากใบแจ้งยอด

**กฎอัตโนมัติไม่มีวันครอบคลุมครบ** วันหยุดพิเศษที่ ครม. ประกาศกะทันหันหรือธนาคารเลื่อนเองจะหลุดเสมอ → ต้องมีกลไก override รายงวด ดูข้อ 1.4.3

**ปฏิทินวันหยุดเป็น option** `roll_calendar: 'weekend_only' | 'weekend_and_bank_holidays'` ดูข้อ 1.4.3

### 1.4.2 ✅ ตัดสินใจแล้ว: งวดแรก (stub period) — field ที่ผู้ใช้แก้ได้

`contract_date`, วันเบิกเงินกู้ และ `first_due_date` มักไม่ตรงกัน งวดแรกจึงอาจสั้นหรือยาวกว่า 1 เดือน — **นี่คือสาเหตุอันดับหนึ่งที่กระทบยอดงวดแรกไม่ตรง**

**`first_accrual_date` เป็น field แยกต่างหาก มีค่า default แต่ผู้ใช้แก้ได้เสมอ** ห้าม derive แบบตายตัวจาก field อื่น

```ts
first_accrual_date: ISODate   // default = วันเบิกเงินกู้ (เงินออกจากธนาคารวันนั้น)
                              // ถ้าไม่รู้วันเบิก fallback เป็น contract_date
```

ใน UI ต้องแสดงเป็นช่องที่แก้ได้พร้อมข้อความว่า
> ถ้างวดแรกในใบแจ้งยอดไม่ตรง ลองปรับวันเริ่มคิดดอกเบี้ยตรงนี้

เหตุผลที่ให้แก้แทนที่จะเดาให้ถูก: งวดแรกเป็น**เหตุการณ์เดียวตลอดสัญญา** ปรับครั้งเดียวจบ การไปไล่หากฎที่ครอบคลุมทุกธนาคารไม่คุ้มกับความซับซ้อน

### 1.4.3 ✅ ตัดสินใจแล้ว: แก้วันตัดรายงวดได้เอง (override)

กฎอัตโนมัติครอบคลุมได้ราว 90% แต่ไม่มีวันครบ — วันหยุดพิเศษที่ ครม. ประกาศกะทันหัน ธนาคารเลื่อนเอง ระบบล่ม ฯลฯ
จึงต้องให้ผู้ใช้ **แก้วันตัดของงวดใดงวดหนึ่งได้โดยไม่กระทบงวดอื่น**

#### การ resolve 3 ชั้น — pattern เดียวกับ `prepay_overrides`

```
nominal(n)  = addMonths(contract_start, n, due_day_of_month)   // clamp สิ้นเดือน
rolled(n)   = roll(nominal(n), date_roll, roll_calendar)
actual(n)   = schedule_overrides[n] ?? rolled(n)               // override ชนะเสมอ
```

accrual ของงวด n คือช่วง `[actual(n−1), actual(n))` — ปลายเปิด

#### ตัวอย่างเดินตัวเลข — จุดที่พลาดง่ายที่สุด

`[MEASURED]` เงินต้น 3,000,000 @5.00% ตัดวันที่ 5 ทุกเดือน แล้ว **override งวด 12 เป็นวันที่ 3**

| | ช่วงคิดดอก | วัน | ดอกเบี้ย |
|---|---|---|---|
| **ปกติ** งวด 12 | 5 ต.ค. → 5 พ.ย. | 31 | 12,739.73 |
| **ปกติ** งวด 13 | 5 พ.ย. → 5 ธ.ค. | 30 | 12,328.77 |
| | | **61** | |
| **override** งวด 12 | 5 ต.ค. → **3 พ.ย.** | **29** | 11,917.81 |
| **override** งวด 13 | **3 พ.ย.** → 5 ธ.ค. | **32** | 13,150.68 |
| | | **61** | |

**งวดที่ถูกเลื่อนสั้นลง 2 วัน งวดถัดไปยาวขึ้น 2 วันมาชดเชยพอดี ผลรวมวันเท่าเดิม** แล้วงวด 14 กลับเป็นปกติ
ถ้า implement แล้วผลรวมวันไม่เท่าเดิม แปลว่ามีวันหายหรือนับซ้ำ = ผิด

#### หมายเหตุเรื่องวิธีเขียนช่วง

`[d0, d1)` กับ `(d0, d1]` **ให้จำนวนวันเท่ากันเสมอ** ต่างกันแค่ว่าวันตัดถูกนับเป็นวันสุดท้ายของงวดเก่าหรือวันแรกของงวดใหม่:

| | งวด 12 | งวด 13 |
|---|---|---|
| `[d0, d1)` ← ที่ spec ใช้ | 5 ต.ค. ถึง 2 พ.ย. = **29** | 3 พ.ย. ถึง 4 ธ.ค. = **32** |
| `(d0, d1]` | 6 ต.ค. ถึง 3 พ.ย. = **29** | 4 พ.ย. ถึง 5 ธ.ค. = **32** |

ทั้งสองแบบได้ 29 วันที่ยอดหนี้ก่อนจ่าย + 32 วันที่ยอดหนี้หลังจ่าย → ดอกเบี้ยเท่ากันทุกสตางค์
**engine ใช้ `[d0, d1)` แบบเดียวตลอด ห้ามสลับ** แต่คอลัมน์ `วันที่คิดดอก` ในตารางผ่อน (ข้อ 3.2) ควรแสดงให้ตรงรูปแบบที่ใบแจ้งยอดของธนาคารใช้ เพื่อให้ผู้ใช้เทียบกับเอกสารจริงได้โดยไม่ต้องแปลงในหัว

```ts
accrual_display: 'start_inclusive' | 'end_inclusive'   // มีผลกับการแสดงผลเท่านั้น ห้ามมีผลกับการคำนวณ
```

#### ⛔ กฎเหล็ก: anchor ต้องอยู่กับที่

`nominal(n)` ต้องนับจาก **วันเริ่มสัญญาเสมอ** ห้ามนับต่อจาก `actual(n−1)`

`[MEASURED]` ตัดวันที่ 5, `preceding`, ปฏิทินเสาร์-อาทิตย์+วันหยุดธนาคาร, 30 ปี:

| | งวด 1 | งวด 120 | งวด 360 | วันที่ที่เกิดขึ้นจริง |
|---|---|---|---|---|
| anchor คงที่ (ถูก) | 2026-11-05 | 2036-10-**03** | 2056-10-**05** | เฉพาะวันที่ 2–5 |
| anchor เลื่อนตาม (ผิด) | 2026-11-05 | 2036-07-**25** | 2056-02-**24** | **ครบทั้ง 1–31** |

ถ้า anchor เลื่อน วันตัดจะไหลจากวันที่ 5 ไปจนถึงวันที่ 1 ภายใน 30 ปี ซึ่งไม่ตรงกับสัญญาใดในโลกจริง
override ก็เช่นกัน — **override งวดหนึ่งห้ามทำให้งวดถัดไปขยับ**

#### Guard ที่ต้อง validate

```
actual(n−1)  <  override(n)  <  nominal(n+1)
```

ถ้าตั้ง override เร็วกว่าวันตัดของงวดก่อนหน้า จะได้ `accrualDays` ติดลบ → ดอกเบี้ยติดลบ → เงินต้นโตขึ้นโดยไม่มีเหตุ
ต้องบล็อกที่ฟอร์ม ไม่ใช่ปล่อยให้ engine คำนวณแล้วออกเลขประหลาด

#### ปฏิทินวันหยุดก็เป็น option

ไม่ใช่ทุกคนอยากดูแลปฏิทินวันหยุดธนาคาร ให้เลือกได้:

```ts
roll_calendar: 'weekend_only'                // เสาร์-อาทิตย์เท่านั้น ไม่ต้องมีข้อมูลเพิ่ม  ← default
             | 'weekend_and_bank_holidays'   // ต้องมีตาราง bank_holidays
```

`[MEASURED]` จำนวนงวดที่สองปฏิทินให้ผลต่างกัน (= จำนวนครั้งที่ต้อง override มือถ้าใช้ `weekend_only`) ขึ้นกับวันตัดอย่างมาก:

| วันตัด | ต่างกัน / 360 งวด | |
|---|---|---|
| 25 | 4 (1.1%) | แทบไม่ต้องทำอะไร |
| 5 | 20 (5.6%) | |
| 28 | 22 (6.1%) | |
| 15 | 35 (9.7%) | |
| 1 | **47 (13.1%)** | ควรใช้ปฏิทินเต็ม |

→ UI ควรคำนวณตัวเลขนี้ให้ดูตอนผู้ใช้เลือกวันตัด แล้วแนะนำว่าควรใช้ปฏิทินแบบไหน

#### UX

แก้ได้จาก **ตารางผ่อนโดยตรง** แตะที่วันในแถวนั้นแล้วเลือกวันใหม่ ไม่ต้องเข้าหน้าตั้งค่า
แถวที่ถูก override ต้องมีเครื่องหมายกำกับและกดคืนค่าเดิมได้ เพื่อให้แยกออกว่าอันไหนมาจากกฎ อันไหนคนแก้

#### ผลต่อ Track mode

**Track mode แทบไม่ได้รับผลกระทบ** เพราะผู้ใช้บันทึกวันที่จ่ายจริงอยู่แล้ว daily engine ใช้วันนั้นตรง ๆ
กลไกทั้งหมดในหัวข้อนี้มีไว้สำหรับ **ตารางที่ projection ไปข้างหน้า** และสำหรับ Compare/Refinance ที่ยังไม่มีการจ่ายจริง

### 1.5 การโปะ (Prepayment)

เงินที่จ่ายเกินค่างวดจะไปตัดเงินต้นโดยตรง [SOURCE: AP Thai, Refinn, DDproperty]

2 โหมด — ธนาคารส่วนใหญ่ให้เลือกได้ [SOURCE: nayoo.co]:
```ts
prepay_mode: 'shorten_term'      // ค่างวดเท่าเดิม ระยะเวลาสั้นลง ประหยัดดอกเบี้ยสูงสุด ← default
           | 'reduce_installment' // ระยะเวลาเท่าเดิม ค่างวดลดลง ประหยัดน้อยกว่า
```
Default = `shorten_term` และแอพควรแสดงผลต่างของ 2 โหมดเป็นตัวเลขจริง ไม่ใช่แค่อธิบาย

**ประเด็นเรื่องวันที่โปะ:** โปะเร็วขึ้น 1 วัน = ประหยัดดอกเบี้ย 1 วันของยอดที่โปะ แอพต้องรองรับการบันทึกวันที่จ่ายจริง (ไม่ใช่แค่ "งวดเดือน ก.ย.") เพราะนี่คือจุดที่ daily engine ให้ค่ามากกว่า monthly engine

**⚠️ ต้องยืนยัน:** มีรายงานจาก Pantip ว่าการจ่าย 2 ครั้งในวันเดียวกัน (ค่างวด + โปะ) ธนาคารตัดเงินต้นได้น้อยกว่าจ่ายครั้งเดียวรวม → พฤติกรรมการตัดชำระต่างกันตามธนาคาร นี่คือเหตุผลที่ต้องมีโมดูล reconciliation (ข้อ 4.3) ห้ามเชื่อ engine อย่างเดียว

### 1.6 ค่าปรับไถ่ถอนก่อนกำหนด

โดยทั่วไป **2–3% ของยอดหนี้คงเหลือ** ถ้าไถ่ถอน/รีไฟแนนซ์ก่อนครบ 3 ปี (บางสัญญา 5 ปี) [SOURCE: ธปท., ธอส., Krungsri]

```ts
lockin_months: number        // default 36
prepay_penalty_bps: number   // default 300 = 3.00% — ⚠️ ต้องอ่านจากสัญญาจริง
```

**สำคัญ:** ค่าปรับนี้ปกติเก็บตอน **ไถ่ถอนหลักประกัน/ย้ายธนาคาร** ไม่ใช่ตอนโปะบางส่วนแล้วอยู่ที่เดิม
→ ในแอพต้องแยก event เป็น `partial_prepay` (ไม่มีค่าปรับ) vs `full_redemption` (มีค่าปรับ) ห้ามรวมกัน

### 1.7 ค่าธรรมเนียมและประกัน — โมเดลต้นทุนเต็ม

ค่าใช้จ่ายมี **3 ประเภทที่คำนวณคนละแบบ** อย่าเก็บรวมเป็น list เดียว

#### ประเภท A — ค่าธรรมเนียมครั้งเดียวตอนทำสัญญา

| รายการ | ฐานคำนวณ | เพดาน | สถานะ |
|---|---|---|---|
| ค่าจดจำนอง (กรมที่ดิน) | 1% ของวงเงินกู้ | — | [SOURCE: DDproperty, Refinn] ⚠️ รัฐเคยมีมาตรการลดชั่วคราว → เปิดให้ override |
| ค่าอากรแสตมป์ | 0.05% ของวงเงิน | **ไม่เกิน 10,000** | [SOURCE: DDproperty, CIMB Thai] |
| ค่าประเมินหลักประกัน | จำนวนคงที่ | — | ⚠️ แล้วแต่ธนาคาร กรอกมือ |
| ค่าธรรมเนียมจัดการสินเชื่อ | แล้วแต่ธนาคาร | — | กรอกมือ |

#### ประเภท B — ประกันที่เกิดซ้ำตลอดอายุสัญญา ← **ส่วนที่เครื่องคิดเลขทั่วไปไม่มี**

**ประกันอัคคีภัย** — ต้องทำตามกฎหมาย ต่ออายุทุก 1–3 ปี ตลอด 30 ปี [SOURCE: DDproperty]
เป็น cash flow ซ้ำ ไม่ใช่ค่าใช้จ่ายครั้งเดียว ต้องใส่ใน TCO และใน XIRR

```ts
type RecurringInsurance = {
  kind: 'fire'
  premium_satang: Satang
  term_years: number            // 1 | 3 โดยทั่วไป
  waived_first_n_years: number  // โปรโมชั่น เช่น CIMB ฟรี 3 ปีแรก [SOURCE]
  escalation_bps_per_renewal: number  // ⚠️ default 0 ถ้าไม่รู้ อย่าเดา
}
```

#### ประเภท C — MRTA / MLTA

ต่างจาก B ตรงที่จ่ายเบี้ยก้อนเดียว คุ้มครองหลายปี และ **มักถูกรวมเข้าวงเงินกู้** ซึ่งทำให้ต้นทุนจริงสูงกว่าเบี้ยที่เห็นมาก

`[MEASURED]` ต้นทุนรวมของ MRTA 100,000 บาทที่รวมในวงเงิน (เงินกู้ฐาน 3,000,000 @5.00% ค่างวดคงที่ ผ่อนจนหมด):

| ค่างวด | ต้นทุนรวมจริงของเบี้ย 100,000 | อายุสัญญาที่ยืดออก |
|---|---|---|
| 16,200 (ใกล้ PMT ขั้นต่ำ ~16,105) | **466,685** | +29 งวด |
| 17,000 | 398,087 | +24 งวด |
| 18,000 | 341,615 | +19 งวด |
| 20,000 | 275,268 | +13 งวด |
| 25,000 | 203,989 | +8 งวด |

อ่านตารางนี้ให้ถูก: ตัวเลขนี้**อ่อนไหวมากต่อค่างวด** ยิ่งค่างวดใกล้ขั้นต่ำ ต้นทุนยิ่งพุ่ง เพราะหนี้ก้อนนั้นแทบไม่ถูกตัดจนถึงปลายสัญญา → แอพต้องแสดงเป็นตารางแบบนี้ **ห้ามแสดงเลขเดียว**

```ts
type CreditLifeInsurance = {
  kind: 'MRTA' | 'MLTA'
  premium_satang: Satang
  coverage_years: number          // มักสั้นกว่าอายุสัญญา เช่น 15 ปีจาก 30
  financed: boolean               // รวมในวงเงินกู้หรือจ่ายสด
  rate_discount_bps: number       // ส่วนลดดอกเบี้ยที่ได้จากการทำ
  is_required: boolean            // บังคับ หรือแค่จูงใจ
  surrender_value_bps?: number    // ⚠️ ดูข้อ 1.8
}
```

**ต้อง compare 2 scenario เสมอ** ไม่ใช่เลือกให้ผู้ใช้:
- ทำ MRTA → ดอกเบี้ยลด `rate_discount_bps` แต่มีเบี้ย (+ ดอกเบี้ยของเบี้ยถ้า financed)
- ไม่ทำ MRTA → ดอกเบี้ยสูงกว่า แต่ไม่มีเบี้ย

แล้วแสดงว่าอันไหนถูกกว่าเท่าไหร่ ณ ปีที่ 3 / ปีที่ 10 / ตลอดสัญญา
**หมายเหตุที่ต้องขึ้นใน UI:** การเทียบนี้เป็นเรื่องต้นทุนอย่างเดียว ไม่ได้ประเมินมูลค่าความคุ้มครองชีวิต ผู้ใช้ต้องตัดสินใจส่วนนั้นเอง

#### โมเดลส่วนลด/ของแถม — ต้องรองรับ "ฟรีแบบมีเพดาน"

ตัวอย่างจริง (CIMB Thai, โปรรีไฟแนนซ์) [SOURCE]: ฟรีค่าประเมิน + ฟรีอากรแสตมป์ 0.05% (ไม่เกิน 10,000) + ฟรีเบี้ยประกันอัคคีภัย 3 ปีแรก + **ฟรีค่าจดจำนอง 1% ของวงเงินกู้ สูงสุดไม่เกิน 100,000**

ข้อสุดท้ายสำคัญ: ถ้ากู้เกิน 10 ล้าน เพดาน 100,000 จะไม่ครอบคลุมทั้งหมด → schema ต้องมี `waiver_cap_satang` ไม่ใช่แค่ boolean

```sql
offer_fees (
  id, offer_id, fee_type, basis,        -- 'flat' | 'pct_of_loan'
  amount_satang, pct_bps,
  cap_satang,                            -- เพดานของตัวค่าธรรมเนียมเอง (เช่น อากรแสตมป์ 10,000)
  is_waived boolean, waiver_cap_satang,  -- เพดานของ "ฟรี"
  is_financed boolean,                   -- รวมในวงเงินกู้หรือจ่ายสด
  clawback_months int,                   -- ⚠️ ถ้าปิดก่อน N เดือนต้องคืนของแถม
  note
)
```

**⚠️ ต้องยืนยันจากสัญญา:** โปรโมชั่น "ฟรี" หลายตัวมีเงื่อนไข clawback ถ้าปิดบัญชีก่อนกำหนด ต้องจ่ายคืน → กระทบการคำนวณ break-even ของการรีไฟแนนซ์โดยตรง

### 1.8 การเวนคืน MRTA เมื่อรีไฟแนนซ์

เมื่อย้ายธนาคาร MRTA เดิมไม่ผูกกับธนาคารใหม่ มี 2 ทางเลือก [SOURCE: DDproperty, Ocean Life]:

1. **เปลี่ยนชื่อผู้รับผลประโยชน์** เป็นธนาคารใหม่ — เก็บความคุ้มครองเดิม ไม่ได้เงินคืน
2. **เวนคืนกรมธรรม์** — ได้เงินคืนตามมูลค่าเวนคืน (ไม่ใช่เบี้ยคงเหลือ) แล้วนำไปทำฉบับใหม่ หรือเอาไปโปะหนี้

มูลค่าเวนคืน **ประมาณ 30–50% ของเบี้ยที่จ่ายไป** ขึ้นกับระยะเวลาที่คุ้มครองไปแล้วและเงื่อนไขแต่ละบริษัท [SOURCE: frasersproperty]

**⚠️ ห้ามใช้ 30–50% เป็นค่า default ในการคำนวณ** ช่วงนี้กว้างเกินกว่าจะใช้ตัดสินใจ — ให้ผู้ใช้กรอกตัวเลขที่ได้จากบริษัทประกันจริง และถ้ายังไม่มี ให้แสดงผลลัพธ์เป็น **ช่วง** ที่ 30% และ 50% พร้อมบอกว่าตัวเลขนี้อาจพลิกข้อสรุปได้

เงินเวนคืนเข้าสูตร break-even ในฐานะ **cash inflow ณ วันรีไฟแนนซ์** → ลดต้นทุนการย้ายลงโดยตรง

### 1.9 ลดหย่อนภาษี

ดอกเบี้ยเงินกู้เพื่อที่อยู่อาศัย ลดหย่อนได้ตามจ่ายจริง **ไม่เกิน 100,000 บาท/ปีภาษี** [SOURCE: กรมสรรพากร ผ่าน ttb / Krungsri / Krungthai] รีไฟแนนซ์ยังใช้สิทธิได้ปกติ [SOURCE: ttb]

กรณีกู้ร่วม: เฉลี่ยตามจำนวนผู้กู้ รวมกันไม่เกิน 100,000 [SOURCE: Krungthai] แต่ถ้าต่างคนต่างกู้ ได้คนละ 100,000 [SOURCE: tb.co.th]

#### ⛔ เพดาน 100,000 เป็นของ "คน" ไม่ใช่ของ "สัญญา"

แอพรองรับหลายสินเชื่อต่อผู้ใช้ (ดูข้อ 7) จุดนี้จึงพังง่ายมากและพังแบบเงียบ

ถ้าทำมิเตอร์ `ดอกเบี้ยปีนี้ / 100,000` แยกรายสัญญา ผู้ใช้ที่มีบ้าน 2 หลังจะเห็นมิเตอร์สองอันที่ยังไม่เต็มทั้งคู่ แล้วเข้าใจว่าใช้สิทธิได้รวมกัน 200,000

```
บ้าน A ปี 2571 ดอกเบี้ย 85,000  →  มิเตอร์แสดง 85,000 / 100,000
บ้าน B ปี 2571 ดอกเบี้ย 60,000  →  มิเตอร์แสดง 60,000 / 100,000
ผู้ใช้เข้าใจว่าลดหย่อนได้        145,000
ความจริงลดหย่อนได้               100,000     ← ส่วนต่างที่เข้าใจผิด 45,000
```

**กฎที่ engine ต้องบังคับ:**
- มิเตอร์สิทธิลดหย่อนคำนวณจาก **ผลรวมดอกเบี้ยทุกสัญญาของผู้ใช้ในปีภาษีนั้น** เทียบกับเพดานเดียว
- หน้าของสัญญาเดี่ยวให้แสดงเป็น "สัญญานี้มีส่วนร่วม X จากเพดานรวม 100,000" ห้ามแสดงเป็นมิเตอร์เต็ม 100,000 ของตัวเอง
- กราฟที่ 6 ในข้อ 4.2 ก็ต้องเป็นยอดรวมข้ามสัญญา ไม่ใช่รายสัญญา
- `ROI หลังภาษี` ในข้อ 3.4 ต้องดูจากเพดานรวมด้วย ไม่งั้นจะบอกว่าโปะแล้วเสียสิทธิทั้งที่สิทธิเต็มไปแล้วจากอีกสัญญา

```ts
tax_config: {
  is_joint_loan: boolean
  borrower_count: number
  marginal_tax_rate_bps: number   // ผู้ใช้กรอก เช่น 2000 = 20% เพื่อคำนวณ "ประหยัดภาษีจริง" เป็นบาท
}
// เปอร์เซ็นต์ทุกตัวในระบบเป็น bps integer ดูกฎชนิดข้อมูลในข้อ 7
```

---

## 2. Compare Mode — จัดอันดับให้ถูก

### 2.1 ปัญหาของวิธีเปรียบเทียบที่ใช้กันทั่วไป

เว็บเปรียบเทียบสินเชื่อไทยเกือบทั้งหมดจัดอันดับด้วย **"ดอกเบี้ยเฉลี่ย 3 ปี"** ซึ่งผิดเพราะ:
1. ไม่รวมค่าธรรมเนียมและ MRTA
2. ไม่สนใจ spread หลังพ้นโปร (ซึ่งกินเวลา 27 ปีจาก 30)
3. ค่างวดแต่ละธนาคารไม่เท่ากัน → เป็นการเทียบ cash flow คนละชุด

### 2.2 Equal-Payment Mode (ฟีเจอร์หลักที่ทำให้แอพนี้ต่าง)

ให้ผู้ใช้ล็อก **ค่างวดรายเดือนเท่ากันทุกธนาคาร** (เช่น 20,000 บาท) แล้วเทียบว่า ณ เดือนที่ 36:

```
Net Position @ M36 = เงินต้นคงเหลือ ณ M36
                   + เงินสดที่จ่ายจริงสะสม   (เฉพาะรายการที่ is_financed = false)
                   − ของแถมที่เป็นเงินสดจริง
```

ต่ำสุด = ชนะ เพราะเงินไหลออกต่อเดือนเท่ากันแล้ว ที่เหลือคือ "จ่ายเท่ากัน ใครทำให้หนี้เหลือน้อยกว่า"

**⛔ ห้ามบวกรายการที่ `is_financed = true` เข้าไปอีก** เพราะถูกรวมในวงเงินกู้ไปแล้ว จึงอยู่ในเงินต้นคงเหลือเรียบร้อย — บวกซ้ำคือ double counting

`[MEASURED]` ตัวอย่างที่สูตรผิดทำให้อันดับพลิก (เงินกู้ฐาน 3,000,000 @5.00% ค่างวดล็อก 20,000 ทั้งคู่ ค่าธรรมเนียมเงินสด 40,000 เท่ากัน):

| | X: MRTA 100,000 **รวมในวงเงิน** | Y: MRTA 100,000 **จ่ายสด** |
|---|---|---|
| เงินต้นคงเหลือ @M36 | 2,825,934.72 | 2,709,771.68 |
| Net Position (สูตรถูก) | **2,865,934.72** | 2,849,771.68 |
| Net Position (บวก MRTA ซ้ำ) | 2,965,934.72 | 2,849,771.68 |

สูตรถูก X แพ้ Y อยู่ 16,163 — สูตรที่บวกซ้ำทำให้ X แพ้ 116,163 คือลงโทษข้อเสนอที่ financed ด้วยเงิน 100,000 ที่ไม่มีอยู่จริง

ถ้าค่างวดที่ล็อกต่ำกว่าค่างวดขั้นต่ำของธนาคารไหน → mark ว่า infeasible ไม่ใช่ซ่อน

### 2.3 Metric ที่ต้องแสดงครบ (ห้ามแสดงแค่ตัวเดียว)

| Metric | ทำไมต้องมี |
|---|---|
| Net Position @ M36 | metric จัดอันดับหลัก |
| ดอกเบี้ยจ่ายรวม 36 งวด | คนไทยคุ้นเคย เอาไว้ cross-check |
| เงินต้นคงเหลือ @ M36 | ฐานสำหรับค่าปรับ + ยอดรีไฟแนนซ์ |
| ค่างวดขั้นต่ำเดือนที่ 1 | ทดสอบว่าจ่ายไหวไหม |
| ค่างวดขั้นต่ำเดือนที่ 37 (พ้นโปร) | **shock test** — ตัวเลขนี้คนมักไม่ดู |
| EIR / XIRR 3 ปี | รวมค่าธรรมเนียมเป็น % เทียบง่าย |
| ดอกเบี้ยรวมตลอดอายุ (ที่ MRR ปัจจุบัน) | แสดงเป็นช่วง ไม่ใช่เลขเดียว |

EIR ใช้ XIRR บน cash flow จริง: `t0 = +เงินกู้ − ค่าธรรมเนียมที่จ่ายสด`, `t1..tn = −ค่างวด`, `tn = −เงินต้นคงเหลือ`

### 2.4 Sensitivity ต่อ MRR

แสดงทุก metric ที่มีช่วงลอยตัวเป็น **แถบ ไม่ใช่จุด** โดยรัน scenario MRR −0.50 / base / +0.50 / +1.00

ถ้าการจัดอันดับพลิกที่ MRR สมมติต่างกัน → ต้องบอกผู้ใช้ตรง ๆ ว่า "อันดับ 1–2 พลิกกันเมื่อ MRR ขยับเกิน +X%" อย่าซ่อนความไม่แน่นอนใต้ตัวเลขเดียว

---

### 2.5 Preset ธนาคารไทย

Seed ตาราง `banks` ด้วยรายชื่อจริง ผู้ใช้เลือกจาก dropdown ไม่ต้องพิมพ์เอง

**ธนาคารพาณิชย์** — ค่า MRR จากตารางประกาศธนาคารพาณิชย์ ณ 7 สิงหาคม 2569 (ที่มา: อินโฟเควสท์) `[SOURCE]`

| ชื่อ | bank_code | MRR |
|---|---|---|
| กรุงเทพ | BBL | 6.500 |
| ไทยพาณิชย์ | SCB | 6.575 |
| กสิกรไทย | KBANK | 6.580 |
| กรุงศรีอยุธยา | BAY | 6.670 |
| กรุงไทย | KTB | 6.845 |
| ทหารไทยธนชาต | TTB | 7.105 |
| ยูโอบี | UOB | 8.075 |

**ต้องเพิ่มแต่ยังไม่มีค่า MRR ในมือ** — ให้ seed ชื่อไว้ก่อน ปล่อยค่าอัตราว่าง:
ธอส. (GHB), ออมสิน (GSB), ซีไอเอ็มบี ไทย (CIMBT), แลนด์ แอนด์ เฮ้าส์ (LHB), ธ.ก.ส. (BAAC)

ธอส. กับออมสินเป็นสถาบันการเงินเฉพาะกิจ ไม่ได้อยู่ในตารางธนาคารพาณิชย์ แต่เป็นตัวเลือกหลักของคนซื้อบ้านไทยเพราะให้วงเงินสูงกว่า จะขาดไม่ได้

**ข้อห้าม:** ห้าม seed อัตราโปรโมชั่นรายผลิตภัณฑ์ลงในระบบ เพราะเปลี่ยนทุกเดือนและขึ้นกับ LTV โปรไฟล์ผู้กู้ และการทำ MRTA ถ้า seed ไปจะกลายเป็นตัวเลขปลอมที่แปะชื่อธนาคารจริง ให้กรอกจากใบเสนอที่ได้มาเท่านั้น

ค่า MRR ที่ seed ไว้ต้องมี `as_of_date` และแสดง badge อายุข้อมูลใน UI ถ้าเกิน 90 วันให้เตือนว่าควรตรวจสอบใหม่

---

## 2A. Refinance Mode — เทียบ 3 ทางเสมอ

ไม่ใช่ feature ย่อย แต่เป็นโหมดหลักเท่ากับ Compare เพราะผู้ใช้จะกลับมาใช้ทุก 3 ปีตลอดอายุสัญญา

### 2A.1 ทางเลือกที่ต้องขึ้นหน้าจอพร้อมกัน

| ทางเลือก | ต้นทุนการย้าย | ข้อดี |
|---|---|---|
| **อยู่เฉย ๆ** | 0 | baseline — ต้องมีเสมอ ห้ามตัดออก |
| **Retention** (ขอลดดอกกับธนาคารเดิม) | ค่าธรรมเนียมเล็กน้อย ไม่มีค่าจดจำนอง ไม่ต้องทำ MRTA ใหม่ ไม่ต้องประเมินใหม่ | เร็ว ถูก ⚠️ อัตราที่ได้มักสู้รีไฟแนนซ์ไม่ได้ |
| **Refinance** (ย้ายธนาคาร) | ค่าปรับ (ถ้ายังไม่พ้น lock-in) + ค่าจดจำนอง 1% + อากรแสตมป์ + ค่าประเมิน + MRTA ใหม่ + ประกันอัคคีภัยใหม่ − เงินเวนคืน MRTA − ของแถม | อัตราต่ำสุด |

**⚠️ ต้องยืนยัน:** ค่าธรรมเนียม retention ต่างกันมากตามธนาคารและไม่ประกาศเป็นมาตรฐาน → กรอกมือ ห้ามใส่ default

### 2A.2 กับดักที่แอพต้องเตือน — ยืดเทอมกลับเป็น 30 ปี

`[MEASURED]` เงินกู้ 3,000,000 เริ่ม ต.ค. 2026, โปร 3 ปี 3.00% แล้วลอยตัว 5.50%, ค่างวด 17,500
ยอดคงเหลือสิ้นปีที่ 3 = **2,624,036.54** สมมติรีไฟแนนซ์ได้โปรชุดเดิม (3 ปี 3.00% → 5.50%)

| ทางเลือก | ดอกเบี้ยที่จ่ายต่อจากนี้ | จำนวนงวดที่เหลือ |
|---|---|---|
| (ก) รีไฟแนนซ์ **แล้วลดค่างวดเหลือ 15,000** | 1,920,701 | 303 |
| (ข) รีไฟแนนซ์ **คงค่างวด 17,500** | **1,322,382** | 226 |
| (ค) ไม่รีไฟแนนซ์ คงค่างวด 17,500 | 1,826,714 | 255 |

- (ข) ประหยัดกว่า (ค) **504,333 บาท** → รีไฟแนนซ์คุ้มมาก
- (ก) แพงกว่า (ค) **93,987 บาท** → **รีไฟแนนซ์ไปดอกเบี้ยถูกกว่า แต่กลับจ่ายดอกเบี้ยรวมมากกว่าการไม่ทำอะไรเลย**

นี่คือสิ่งที่ธนาคารขายว่า "ค่างวดลดลง 2,500 บาท/เดือน" แล้วผู้กู้ไม่เห็นว่าแลกมาด้วยอะไร
→ **หน้าผลลัพธ์ต้อง default เป็นโหมดคงค่างวด** และถ้าผู้ใช้เลือกลดค่างวด ต้องขึ้นตัวเลขส่วนต่างเทียบกับการไม่ทำอะไรทันที ไม่ใช่ซ่อนใน tooltip

### 2A.3 สูตร Break-even

```
ต้นทุนการย้าย = ค่าปรับไถ่ถอน (ถ้ายังไม่พ้น lock-in)
              + ค่าจดจำนองใหม่ + อากรแสตมป์ + ค่าประเมิน + ค่าธรรมเนียมอื่น
              + เบี้ย MRTA ใหม่ (ถ้าทำ)
              + เบี้ยประกันอัคคีภัยใหม่ (ถ้าไม่ฟรี)
              + ของแถมที่ต้องคืน (clawback ของสัญญาเดิม)
              − เงินเวนคืน MRTA เดิม
              − ของแถมจากธนาคารใหม่

เดือนคืนทุน = เดือนแรกที่  Σ(ดอกเบี้ยที่ประหยัดสะสม)  >  ต้นทุนการย้าย
```

โดยที่ "ดอกเบี้ยที่ประหยัด" ต้องเทียบบน **ค่างวดเท่ากัน** ไม่งั้นได้เลขหลอก

**Rule ที่ต้องเขียนใน engine:** ถ้าเดือนคืนทุน > `lockin_months` ของข้อเสนอใหม่ → ตอบว่า **ไม่คุ้ม** ตรง ๆ เพราะพอถึงจุดคุ้มทุนคุณก็ต้องรีไฟแนนซ์รอบใหม่อยู่ดี

### 2A.4 Refinance Chain

สัญญาไม่ใช่ entity เดียวตลอดชีวิต ต้องเก็บเป็นโซ่:

```sql
active_loans (
  ...,
  supersedes_loan_id uuid null,   -- สัญญาก่อนหน้าที่ถูกรีไฟแนนซ์มา
  origin text,                     -- 'new_purchase' | 'refinance' | 'retention'
  closed_date date null,
  closing_reason text              -- 'refinanced' | 'paid_off' | null
)
```

ผลลัพธ์: Dashboard ต้องรวมทั้งโซ่ได้ → "ตั้งแต่ซื้อบ้านมา 9 ปี จ่ายดอกเบี้ยไปแล้วรวม X บาท ข้ามมา 3 ธนาคาร" ซึ่งเป็นตัวเลขที่ไม่มีที่ไหนบอกได้

Retention = สร้าง record ใหม่ที่ `origin='retention'` และ `supersedes` สัญญาเดิม โดย**ไม่**มีค่าจดจำนอง/MRTA ใหม่ และ**ไม่** reset `contract_date` สำหรับการนับ lock-in เดิม
**⚠️ ต้องยืนยัน:** retention จะ reset lock-in period ใหม่หรือไม่ ขึ้นกับบันทึกต่อท้ายสัญญา — ทำเป็น flag `resets_lockin: boolean`

### 2A.5 Import สัญญาที่ผ่อนไปแล้ว

จำเป็นสำหรับโหมดรีไฟแนนซ์ ต้องมีตั้งแต่ M2

2 วิธี ให้ผู้ใช้เลือก:
- **Quick** — กรอก `ยอดคงเหลือปัจจุบัน` + `วันที่ของยอดนั้น` + `ค่างวด` + `อัตราปัจจุบัน` + `วันครบสัญญาเดิม` → ใช้เป็นจุดตั้งต้น ไม่มีประวัติย้อนหลัง
- **Full** — กรอกวันทำสัญญา + วงเงินเดิม + rate steps + รายการจ่ายย้อนหลัง → ได้ประวัติครบ ใช้ reconcile ได้

Quick ต้อง flag ใน UI ว่าตัวเลขดอกเบี้ยสะสมและสิทธิลดหย่อนภาษีย้อนหลังจะไม่มี

---

## 3. Track Mode

### 3.1 หลักการ: ผู้ใช้กรอกแค่ "จ่ายเมื่อไหร่ เท่าไหร่"

**ห้ามให้ผู้ใช้กรอกเองว่าแบ่งเป็นต้นเท่าไหร่ดอกเท่าไหร่** — ระบบคำนวณให้ เพราะการแบ่งขึ้นกับจำนวนวันจริงระหว่างงวด ซึ่งคนคำนวณเองแล้วผิดแน่นอน

Input ต่อรายการ: `วันที่จ่าย`, `จำนวนเงิน`, `ช่องทาง`, `หมายเหตุ`
Output: `ดอกเบี้ย`, `เงินต้น`, `ยอดคงเหลือหลังจ่าย`, `จำนวนวันที่คิดดอก`

### 3.2 ตารางผ่อน (Amortization Table)

3 layer ซ้อนกันในตารางเดียว สลับดูได้:
- **Plan** — ตารางที่ engine คำนวณจากสัญญา (ไม่มีโปะ)
- **Actual** — จากรายการจ่ายจริงที่บันทึก
- **Statement** — จากใบแจ้งยอด/หนังสือรับรองดอกเบี้ยของธนาคาร (กรอกหรือ import)

คอลัมน์: `งวด | วันที่ | วันที่คิดดอก | อัตรา | จำนวนเงิน | ดอกเบี้ย | เงินต้น | คงเหลือ | Δ vs Statement`

Virtualized list (360 แถว) — ใช้ `@tanstack/react-virtual`

### 3.3 Reconciliation (ฟีเจอร์ที่แอพอื่นไม่มี)

เทียบ Actual vs Statement รายงวด แล้ว flag:
- `|Δ| ≤ 1 บาท` → เขียว (ปัดเศษ)
- `1 < |Δ| ≤ 50` → เหลือง (น่าจะเป็นนโยบายปัดเศษหรือ day-count ต่างกัน)
- `|Δ| > 50` → แดง + แสดงสมมติฐานที่จะทำให้ตรง (เช่น "ถ้าใช้ฐาน 366 วัน จะต่างเหลือ 2 บาท")

มีรายงานว่าธนาคารปัดเศษดอกเบี้ยลงทุกกรณี [SOURCE: promise.co.th — สินเชื่อส่วนบุคคล ⚠️ ยังไม่ยืนยันว่าสินเชื่อบ้านใช้กฎเดียวกัน]
→ ทำเป็น config `interest_rounding: 'floor_baht' | 'floor_satang' | 'round_satang' | 'none'` แล้วมีปุ่ม **"หาโหมดที่ทำให้ตรง statement มากที่สุด"**

#### Inference ต้องทำ 2 ขั้น ห้ามยำรวมกันเป็น brute force ก้อนเดียว

ถ้าโยนทุกมิติเข้าไปพร้อมกัน (4 ปัดเศษ × 3 basis × 3 date_roll × 2 ปฏิทิน × 2 capitalise = 120 แบบ) แล้วมี statement แค่ 12 แถว จะเป็นการ **fit noise** ไม่ใช่ค้นพบความจริง

| ขั้น | ใช้ข้อมูลอะไร | วิธี | จำนวนที่ลอง |
|---|---|---|---|
| **1. วันตัด** | คอลัมน์ **วันที่** ในใบแจ้งยอด | จับคู่แบบตรงตัว ไม่ใช่ fit ตัวเลข — วันที่ที่ธนาคารแจ้งมาคือความจริงอยู่แล้ว เอามาเทียบว่าเข้ากฎข้อไหน | 5 (`none` + `preceding`/`following` × 2 ปฏิทิน) |
| **2. จำนวนเงิน** | คอลัมน์ **ดอกเบี้ย/เงินต้น/ยอดคงเหลือ** | ตรึงวันตัดจากขั้น 1 แล้ว brute force หา MAE ต่ำสุด | **24** (4 ปัดเศษ × 3 basis × 2 capitalise) |

ขั้นที่ 1 แทบไม่มีโอกาสผิดเพราะเทียบวันที่ตรง ๆ ถ้าไม่มีกฎไหนตรงครบ ให้เสนอเป็น override รายงวดแทน (ข้อ 1.4.3)
ขั้นที่ 2 เหลือ 24 แบบซึ่งสมเหตุสมผลกับข้อมูล 12 แถว

**กัน overfit:** ถ้ามีหลาย combination ที่ MAE ต่างกันไม่ถึง 1 บาท ให้แสดงทุกตัวที่เข้าข่ายพร้อมบอกว่าแยกไม่ออก อย่าเลือกให้เงียบ ๆ (เช่น ปีปกติ ACT/365F กับ ACT/ACT แยกไม่ออกโดยหลักการ ดูข้อ 1.1.1)

#### ✅ ตัดสินใจแล้ว: ปัดเศษครั้งเดียวตอนจบงวด

ชื่อโหมดอย่างเดียวไม่พอ ต้องระบุจุดที่ปัดด้วย เพราะต่างกันมากจนเป็นคนละ config

**กฎ: สะสมดอกเบี้ยระหว่างงวดเป็น exact ห้ามปัด แล้วปัดครั้งเดียว ณ จุดตัดชำระ**
เหตุผล: ธนาคารโพสต์ดอกเบี้ยเป็นยอดเดียวต่องวดลงใบแจ้งยอด ซึ่งคือตัวเลขที่เราเอาไปเทียบ ส่วนรายวันเป็นแค่สูตรระหว่างทาง
การปัดรายวันยังทำให้ธนาคารเสียเงินราว 180 บาท/ปีในโหมด floor ซึ่งไม่น่าเป็นพฤติกรรมจริง

`[MEASURED]` P=3,000,000 @5.00% ค่างวด 16,200 เริ่ม 2026-10-01 ดอกเบี้ยรวมตลอดสัญญา:

| โหมด | ปัดจบงวด | ปัดรายวัน | ต่างกัน |
|---|---|---|---|
| `none` | 2,758,560.20 | — | — |
| `round_satang` | 2,758,560.08 | 2,758,557.04 | 3.04 |
| `floor_satang` | 2,758,556.11 | 2,758,436.17 | 119.94 |
| `floor_baht` | 2,758,160.00 | 2,745,748.00 (355 งวด) | **12,412.00** |

ตัวเลขคอลัมน์ "ปัดรายวัน" เก็บไว้เป็นหลักฐานว่าทำไมถึงต้องระบุจุดที่ปัด **ไม่ต้อง implement โหมดปัดรายวัน**

### 3.4 Prepay Planner — วางแผนโปะล่วงหน้า

ผู้ใช้ต้องตั้งยอดโปะ **ไม่เท่ากันในแต่ละเดือน** ได้ เช่น ม.ค. 5,000 / ก.พ. 5,500 / มี.ค. 5,000 / เม.ย. ถึง พ.ย. เดือนละ 4,000 / ธ.ค. ไม่โปะ

#### ปัญหาที่ต้องแก้: 30 ปีมี 360 เดือน

กรอกมือทั้งหมดไม่ไหว และเก็บเป็น array 360 ช่องก็ผิด ให้เก็บเป็น **แผน 12 เดือน + override รายปี**

```sql
prepay_plans (
  id, scenario_id, base_year int,
  repeat_mode text,        -- 'single_year' | 'repeat_forever' | 'repeat_until'
  repeat_until_year int null
)
prepay_months (id, plan_id, month int, amount_satang bigint)      -- 12 แถวของ base_year
prepay_overrides (id, plan_id, year int, month int, amount_satang) -- เฉพาะปีที่ต่างจาก base
prepay_lumps (id, plan_id, pay_date date, amount_satang, label)    -- ก้อนเดี่ยวตามวันที่
```

การ resolve ยอดโปะของงวดหนึ่ง:
```
amount(year, month) = prepay_overrides[year][month]
                   ?? prepay_months[month]          (ถ้า repeat_mode ครอบคลุมปีนั้น)
                   ?? 0
บวกด้วย prepay_lumps ที่ตรงวันจ่ายงวดนั้น
```

ปุ่ม "คัดลอกไปปีถัดไป" = สร้าง `prepay_overrides` ชุดใหม่จากปีปัจจุบัน

#### UI: 2 มุมมอง สลับได้ state เดียวกัน

**มุมมองปฏิทิน** — ตาราง 3 คอลัมน์ × 4 แถว เห็นครบ 12 เดือนในจอเดียวโดยไม่ต้องเลื่อน แต่ละช่องมีชื่อเดือน ยอด และแถบสัดส่วนใต้ตัวเลขเพื่อให้กวาดตาเห็น "รูปร่างของปี" ได้ทันที

**มุมมองรายการ** — แถวรายงวด แสดงผลลัพธ์จริง: `ค่างวด + โปะ = ยอดจ่าย`, แถบสัดส่วนดอกต่อต้น, `ดอก x · ต้น y`, `เหลือ z` พร้อมป้ายจำนวนวันเมื่อเดือนสั้น

มุมมองรายการต้องแสดงสิ่งที่ปฏิทินแสดงไม่ได้ ไม่ใช่จัดเรียงข้อมูลชุดเดิมใหม่ ถ้าทำได้แค่จัดเรียงใหม่ ไม่ต้องมีสองมุมมอง

```sql
user_prefs (..., prepay_view text)   -- 'calendar' | 'list'  default 'calendar'
```

#### การเลือกช่วง — ข้อกำหนดหลัก

เคส "เม.ย. ยาว ๆ ไป พ.ย." คือเคสที่พบบ่อยที่สุด ถ้าต้องกรอกทีละช่อง 8 ครั้งคือออกแบบพลาด

- แตะเดือนแรก แล้วแตะเดือนสุดท้าย = เลือกเป็นช่วง
- ชิปค่าสำเร็จรูป (3,000 / 4,000 / 5,000 / 10,000 / ไม่โปะ) ยิงค่าเดียวลงทั้งช่วง
- ปุ่ม ±500 ปรับทั้งช่วงพร้อมกัน
- แตะที่ตัวเลขในช่อง = เปิดคีย์แพดพิมพ์ค่าเอง สำหรับยอดที่ไม่มีในชิป
- เลือกช่วงได้จากทั้งสองมุมมอง

#### ผลลัพธ์ที่ต้องอัปเดตสด

- ปิดหนี้เร็วขึ้นกี่งวด
- ประหยัดดอกเบี้ยกี่บาท
- โปะเพิ่มรวมตลอดแผนกี่บาท
- **ROI ต่อบาทที่โปะ** = ดอกเบี้ยที่ประหยัด ÷ เงินที่โปะ ใช้เทียบกับผลตอบแทนการลงทุนทางเลือกได้

ROI ตัวนี้ **ลดลงเมื่อโปะหนักขึ้น** เพราะหนี้หมดเร็วจนไม่เหลือดอกเบี้ยให้ประหยัด ต้องแสดงให้เห็น diminishing return ไม่ใช่บอกแค่ว่าโปะเยอะดี

#### ROI หลังภาษี — ต่างกันตามปี

ปีที่ดอกเบี้ยเกินเพดานลดหย่อน 100,000 การโปะให้ผลเต็ม เพราะดอกส่วนเกินไม่ได้ลดหย่อนอยู่แล้ว
ปีที่ดอกเบี้ยต่ำกว่าเพดาน โปะไป 1 บาทจะเสียสิทธิลดหย่อน 1 บาทด้วย ผลตอบแทนสุทธิต่ำกว่า

```
ROI หลังภาษี = (ดอกที่ประหยัด − ส่วนที่เสียสิทธิลดหย่อน × marginal_tax_rate) ÷ เงินที่โปะ
```
ต้องใช้ `tax_config.marginal_tax_rate_bps` ที่ผู้ใช้กรอก ถ้ายังไม่กรอกให้ซ่อนคอลัมน์นี้ อย่าเดาอัตราภาษี

### 3.5 การจัดกลุ่มตารางผ่อน — 2 แกน

360 งวดเรียงต่อกันใช้งานไม่ได้ ต้องย่อเป็นรายปี แต่ "ปี" มี 2 แบบและ**ต้องมีทั้งคู่**

| แกน | ตอบคำถาม | คอลัมน์ขวาที่ต้องโชว์ |
|---|---|---|
| **ปีสัญญา** (default) | เรตที่ตกลงในสัญญากับที่จ่ายจริงตรงกันไหม | `เรตที่ตกลง → เรตที่จ่ายจริง` พร้อมเครื่องหมายยืนยัน |
| **ปีปฏิทิน** | สิทธิลดหย่อนภาษีปีนี้ใช้เต็มหรือยัง | มิเตอร์ `ดอกเบี้ยปีนี้ / 100,000` |

**toggle นี้ต้องสลับคอลัมน์ขวาด้วย ไม่ใช่แค่จัดกลุ่มใหม่** ถ้าสลับแล้วเห็นข้อมูลชุดเดิม ก็ไม่มีเหตุผลต้องมีสองแกน

#### เหตุผลที่ต้องมีทั้งคู่

อัตราดอกเบี้ยถูกนิยามด้วย `from_month`/`to_month` ซึ่งเป็นเดือนของสัญญา ส่วนเพดานลดหย่อนภาษีคิดเป็นปีปฏิทิน สองแกนนี้ไม่ทับกัน

ตัวอย่างสัญญาที่เริ่ม เม.ย. 2025:
- ปีสัญญาที่ 4 = พ.ค. 2028 ถึง เม.ย. 2029
- ปีภาษี 2028 = ม.ค. ถึง ธ.ค. 2028 ซึ่งคร่อมทั้งปีสัญญาที่ 3 และที่ 4

มีแกนเดียวจะเสียอย่างใดอย่างหนึ่งเสมอ

#### ข้อกำหนดของ engine

ทั้งสองแกนต้องมาจากตารางรายงวด **ชุดเดียวกัน** แล้วค่อยจัดกลุ่มทีหลัง ห้ามคำนวณแยกสองรอบ

```ts
function groupSchedule(
  rows: ScheduleRow[],
  axis: 'contract_year' | 'calendar_year'
): YearGroup[]
```

ผลรวมดอกเบี้ยของทั้งสองแกนต้องเท่ากันทุกสตางค์ (ดู TV-25)

#### ข้อห้าม: ห้ามเฉลี่ยอัตราดอกเบี้ยแบบเลขคณิต

ปีที่มีการเปลี่ยนเรตกลางปี การเอาเรตมาบวกกันหารจำนวนงวดให้ตัวเลขที่ไม่มีความหมาย

```
เรตที่จ่ายจริงของงวดกลุ่มหนึ่ง
  = Σ ดอกเบี้ย ÷ ( Σ (เงินต้นก่อนจ่าย × จำนวนวัน) ÷ 365 )
```

ใช้สูตรนี้กับทั้งการแสดงผลรายปี และการตรวจโฆษณา "ดอกเบี้ยเฉลี่ย 3 ปี" ของธนาคาร

#### ต้องติดป้ายปีไม่เต็ม

ปีแรกและปีสุดท้ายมักมีไม่ครบ 12 งวด ถ้าไม่ติดป้ายจำนวนงวด ผู้ใช้จะเอาไปเทียบกับปีเต็มแล้วสรุปผิด

### 3.6 Scenario เปรียบเทียบ

บันทึกแผนได้หลายชุด เทียบกันได้ เช่น โปะสม่ำเสมอ vs โปะก้อนตอนโบนัส vs รีไฟแนนซ์ปีที่ 4

---

## 4. Dashboard + กราฟ

### 4.1 Hero — ไม่เอาตัวเลขใหญ่ + gradient

Hero คือ **Split Ribbon**: แถบแนวนอนของทุกงวดตลอดอายุสัญญา แต่ละงวดคือแท่งบาง ๆ 2 สี (ดอกเบี้ย / เงินต้น) เรียงเป็นริบบิ้น มีเส้นบอกตำแหน่งงวดปัจจุบัน

ริบบิ้นนี้บอกความจริงที่แอพอื่นซ่อน: ปีแรก ๆ แถบสีดอกเบี้ยกินพื้นที่เกือบทั้งหมด แล้วค่อย ๆ ถอย มี marker ที่ **"งวดที่เงินต้นแซงดอกเบี้ย"** — เป็น milestone ที่มีความหมายกว่า "ผ่อนไปแล้ว 12%"

ใต้ริบบิ้น: ประโยคเดียว ภาษาคน
> งวดนี้จ่าย 20,000 — เป็นดอกเบี้ย 11,240 เข้าเงินต้น 8,760

### 4.2 กราฟที่ต้องมี (แต่ละอันตอบคำถามเดียว)

| # | กราฟ | ตอบคำถาม |
|---|---|---|
| 1 | Split Ribbon (hero) | เงินที่จ่ายไปไหน |
| 2 | Balance: Plan / Actual / No-prepay baseline (3 เส้น) | การโปะช่วยได้แค่ไหน — ระยะห่างคือเงินที่ประหยัด |
| 3 | Cumulative stacked bar รายปี (ดอก/ต้น) | ปีนี้จ่ายไป X จริง ๆ แล้วเป็นดอกกี่บาท |
| 4 | **Bank comparison: cumulative cost curve** (1 เส้น/ธนาคาร, x = เดือน 0–360) | อันดับพลิกที่เดือนไหน — ดีกว่า bar chart เฉลี่ย 3 ปีมาก |
| 5 | MRR sensitivity fan | ถ้า MRR ขึ้น ค่างวด/ดอกเบี้ยจะเป็นยังไง |
| 6 | Tax deduction bar + เส้น cap 100,000 | ปีนี้ใช้สิทธิเต็มหรือยัง ประหยัดภาษีกี่บาท — **รวมทุกสัญญา** ดูข้อ 1.9 |

**ไม่เอา:** donut chart สัดส่วนต้น/ดอก (อ่านค่ายาก), gauge เปอร์เซ็นต์ผ่อน (ทำให้เข้าใจผิดว่าใกล้หมดแล้ว)

---

## 5. Design Direction

> ส่วนนี้คือ art direction ถ้าไม่ชอบเปลี่ยนได้ แต่อย่าเปลี่ยนเป็น SaaS card kit ทั่วไป

### 5.1 แนวคิด

**"กรรมสิทธิ์กำลังย้ายมือ"** — ทุกจำนวนเงินในแอพมี 2 ส่วนเสมอ ส่วนที่หายไปให้ธนาคาร (ดอกเบี้ย) กับส่วนที่กลายเป็นของเรา (เงินต้น) หน่วยพื้นฐานของ UI ไม่ใช่ card แต่เป็น **แถบสองสี** ซึ่งซ้ำเป็น pattern ตั้งแต่ตัวเลขตัวเดียว ไปจนถึงริบบิ้น 360 งวด

### 5.2 Tokens

```css
/* พื้นผิว — เย็นกว่า cream, ไม่ใช่ warm beige */
--paper:        #F5F5F1;
--paper-raised: #FFFFFF;
--rule:         #DCDCD4;   /* เส้นคั่น */

/* หมึก */
--ink:          #161A18;
--ink-2:        #5C625E;
--ink-3:        #717874;   /* แก้จาก #8D938F ซึ่งได้ contrast 2.87 บน --paper = ตก AA */

/* คู่สีความหมาย — ใช้แค่ 2 สีนี้ทั้งแอพ */
--interest:     #2B4570;   /* น้ำเงินหมึก = เงินที่ไหลออกไปธนาคาร */
--interest-t:   #2B457022;
--principal:    #D98B2B;   /* เหลืองส้มแบบจีวร = ส่วนที่เป็นของเรา */
--principal-t:  #D98B2B22;

/* สถานะ — เฉพาะเตือนจริง ๆ */
--warn:         #A8431F;
--ok:           #2F6B4F;

/* แผง hero สีหมึกเข้ม — ใช้เฉพาะส่วนบนของ Dashboard และแถบสรุปผลแผนโปะ */
--panel:        #182742;
--panel-rule:   #2F4165;
--panel-ink:    #FBFAF7;
--panel-ink-2:  #9DB0CC;
--panel-ink-3:  #6F82A0;

/* คู่สีบนพื้นหมึกเข้ม — ต้องสว่างกว่าคู่สีบนพื้นกระดาษ ไม่ใช่ค่าเดียวกัน */
--interest-dk:  #5878AB;   /* แก้จาก #3D5375 ดูหมายเหตุ contrast ด้านล่าง */
--principal-dk: #E8A33D;
--ribbon-rule:  #182742;   /* hairline 1px คั่นสองสีในริบบิ้น = สีเดียวกับพื้นแผง */

/* เงินต้นบนพื้นกระดาษเมื่อใช้เป็นตัวอักษร ต้องเข้มกว่าตอนใช้เป็นแถบสี */
--principal-tx: #8A5A12;
```

`--principal` #D98B2B ใช้ได้ดีเป็นแถบสี แต่ถ้าเอาไปทำตัวอักษรบนพื้นกระดาษจะคอนทราสต์ไม่ผ่าน (2.50) ให้ใช้ `--principal-tx` (5.41) แทน

#### หมายเหตุ contrast ของริบบิ้นบนแผงหมึกเข้ม

`[MEASURED]` ค่าที่ร่างไว้เดิม `--interest-dk #3D5375` ได้ contrast กับพื้นแผง #182742 แค่ **1.91** ซึ่งเป็นปัญหาจริงไม่ใช่เรื่องเทคนิค เพราะ **ปีแรก ๆ ริบบิ้นเป็นสีดอกเบี้ยเกือบทั้งแถบ** → hero จะดูเหมือนแผงเปล่า ซึ่งกลบข้อความที่ข้อ 4.1 ตั้งใจสื่อพอดี

ปัญหาคือสองเงื่อนไขนี้ขัดกัน ไม่มีสีเดียวที่ผ่านทั้งคู่:

| `--interest-dk` | vs พื้นแผง (ต้อง ≥3.0) | vs ทอง `--principal-dk` (ต้อง ≥3.0) |
|---|---|---|
| `#3D5375` เดิม | **1.91** ✗ | 3.62 ✓ |
| `#5878AB` ใหม่ | **3.33** ✓ | 2.08 ✗ |
| `#6083B5` | 3.84 ✓ | 1.80 ✗ |

**ทางออก: ใช้ `#5878AB` แล้วคั่นสองสีด้วย hairline 1px สี `--ribbon-rule`** เส้นคั่นทำหน้าที่แยกสองส่วนแทนค่าคอนทราสต์ระหว่างสี ซึ่งเป็นวิธีมาตรฐานของ stacked bar และทำให้ผ่านทั้งสองเงื่อนไข

⚠️ นี่เป็น art direction ถ้าไม่ชอบเปลี่ยนได้ แต่ **ห้ามกลับไปใช้ค่าที่ contrast กับพื้นแผงต่ำกว่า 3.0** โดยไม่มีวิธีชดเชย

เหตุผลของคู่สี: ปกติ UI การเงินใช้แดง=แย่ เขียว=ดี ซึ่งไม่ตรงกับความจริงที่นี่ (ดอกเบี้ยไม่ใช่ "ผิด" มันคือต้นทุนของการมีบ้านตอนนี้) น้ำเงินหมึก = เอกสารสัญญา เย็น เป็นของธนาคาร / เหลืองส้ม = อบอุ่น เป็นของเรา อ่านความหมายได้โดยไม่ต้องมี legend

**ห้ามใช้:** พื้นครีม #F4F1EA + serif + terracotta #D97757, พื้นดำ + acid green, gradient wash เป็นของตกแต่ง

### 5.3 Typography

| บทบาท | ฟอนต์ | เหตุผล |
|---|---|---|
| Display / หัวข้อ | **Anuphan** (Google Fonts, Cadson Demak) | Thai sans ไม่มีหัว variable weight ไม่ใช่ Sarabun ที่อ่านเหมือนหนังสือราชการ |
| Body / ตาราง | **IBM Plex Sans Thai** | x-height สูง อ่านตัวเลขเยอะ ๆ ได้ มี Latin คู่ที่เข้ากัน |

**ปีทุกจุดใน UI แสดงเป็น พ.ศ.** — ตัดสินใจแล้ว

| ชั้น | ใช้อะไร |
|---|---|
| Database, engine, test vector, API | **ค.ศ. เป็น ISO `YYYY-MM-DD` เสมอ** ห้ามเก็บ พ.ศ. ลง DB เด็ดขาด |
| UI ทุกที่ที่ผู้ใช้เห็น | **พ.ศ.** (`ค.ศ. + 543`) |

ต้องมี helper กลางตัวเดียว ห้ามบวก 543 กระจายตามไฟล์:
```ts
// src/lib/date-th.ts
formatThaiDate(iso: ISODate, style: 'short'|'long'|'monthYear'): string
formatThaiYear(iso: ISODate): string       // 2026-10-01 → '2569'
```
ใช้ `Intl.DateTimeFormat('th-TH-u-ca-buddhist')` เป็นฐาน ห้ามบวกเลขเอง (จัดการขอบปีผิดได้)
ช่องกรอกวันที่ก็ต้องรับ/แสดง พ.ศ. ด้วย แต่แปลงเป็น ISO ก่อนส่งเข้า engine ทุกครั้ง

**ตัวเลขทุกจุดต้องเป็น tabular figures:**
```css
.num { font-variant-numeric: tabular-nums; font-feature-settings: 'tnum' 1; }
```
ห้ามสลับไปใช้ monospace face เพื่อให้เลขตรงคอลัมน์ — ใช้ `tnum` พอ

Line-height ภาษาไทยต้องสูงกว่าอังกฤษ: body `1.7`, ตาราง `1.5` (สระบน/ล่างซ้อนกันชนกันง่าย)

### 5.4 Layout — Dashboard

ร่างแรกวางทุกอย่างบนพื้นครีมทั้งจอ ผลคือทองจมหายกับพื้น และขนาดตัวอักษรใกล้กันหมดจนไม่มีลำดับสายตา แก้โดยยก hero ขึ้นแผงหมึกเข้ม

```
┌═════════════════════════════════┐
║ งวด 18 จาก 226        ต.ค. 2569 ║  พื้น #182742
║                                 ║
║ 2,770,279                       ║  38px  สีกระดาษ
║ ยังเป็นหนี้อยู่เท่านี้             ║  12px
║                                 ║
║ ████▓▓▓▓░░░░░░░░░  ribbon 46px  ║  ทอง #E8A33D บนสเลต #5878AB
║ 2568    พ้นโปร 2571       2587  ║
║ ดอกเบี้ย  เงินต้น   งวด 37–79 ●  ║
└═════════════════════════════════┘
  งวดนี้จ่าย 20,000                   22px
  ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬               แถบแยกดอก/ต้น
  ดอกเบี้ย 6,863.22   เงินต้น 13,136.78
  ───────────────────────────────
  จ่ายไปแล้ว 18 งวด          360,000   17px ชิดขวา
  หายไปกับดอกเบี้ย           130,279
  สิทธิลดหย่อนภาษีปีนี้        58,410
  ▬▬▬▬▬▬▬▬▬░░░░░░  เหลืออีก 41,590
  ───────────────────────────────
  ตามแผนโปะปัจจุบัน ปิดหนี้    เร็วขึ้น
  ก.ย. 2580                  85 งวด    24px ทั้งคู่
  ───────────────────────────────
  ✓ ตรงกับใบแจ้งยอด 18 จาก 18 งวด
```

**ลำดับขนาดตัวอักษรต้องห่างกันจริง** 38 → 24 → 22 → 17 → 13 → 11 ถ้าทุกอย่างอยู่ในช่วง 13–16 หน้าจอจะอ่านยากทันที

- Mobile-first จริง ๆ breakpoint แรกที่ 380px
- ตัวเลขชิดขวา ป้ายชิดซ้าย คั่นด้วยเส้น hairline ห้ามใช้การ์ดขาวลอย
- แถบมิเตอร์ (สิทธิลดหย่อน) ใช้เมื่อมีเพดานชัดเจนเท่านั้น อย่าใช้กับตัวเลขที่ไม่มีเพดาน
- ตารางบนมือถือใช้แถว 3 บรรทัด (หัวแถว / แถบสัดส่วน / รายละเอียด) แทนตารางเลื่อนแนวนอน อ่านง่ายกว่ามากที่ความกว้าง 380px

### 5.5 Motion

จังหวะเดียวที่มี animation: **ตอนบันทึกการจ่ายสำเร็จ** → ริบบิ้นขยับ marker ไป 1 งวด + แถบเงินต้นโตขึ้นให้เห็น (300ms ease-out) เพราะนี่คือสิ่งที่เปลี่ยนจริง
ที่เหลือไม่มี fade-in-on-scroll ไม่มี hover transition บนทุก card

`@media (prefers-reduced-motion: reduce)` → ตัดทั้งหมด

---

## 5A. Responsive Strategy

### 5A.1 ข้อห้ามหลัก

**ห้ามครอบ `max-width: 480px; margin: 0 auto` ไว้ที่ layout ระดับบนสุด** เปิดบน desktop แล้วเห็นคอลัมน์มือถือลอยกลางจอ 27 นิ้วคือการออกแบบที่ล้มเหลว

Breakpoint ต้องเปลี่ยน **โครงสร้างและความหนาแน่นของข้อมูล** ไม่ใช่แค่ขยายขนาด การขยาย font-size อย่างเดียวแล้วคงคอลัมน์เดียวไว้ ถือว่าไม่ผ่าน

### 5A.2 สิ่งที่เปลี่ยนในแต่ละ breakpoint

| Breakpoint | คอลัมน์ | ตารางผ่อน | Compare | Dashboard |
|---|---|---|---|---|
| `< 640` | 1 | แถว 3 บรรทัด | wizard ทีละขั้น | hero เต็มกว้าง กราฟเรียงลง |
| `640–1023` | 2 | ตารางจริง 5 คอลัมน์ | ฟอร์ม 2 คอลัมน์ | hero + สรุปเคียงกัน |
| `1024–1439` | 2–3 | ตารางเต็ม 9 คอลัมน์ ไม่ต้องกาง | คอลัมน์ต่อธนาคารเรียงข้างกัน | hero + กราฟ 2 ใบเคียงกัน |
| `≥ 1440` | 3 | เพิ่มคอลัมน์ Δ กระทบยอดถาวร | เห็นครบ 4 ธนาคารพร้อมกัน | ริบบิ้นกว้างขึ้น เห็น marker ชัดขึ้น |

`max-width` ของ content อยู่ที่ 1440px ไม่ใช่ 480px หน้าที่เป็นตารางล้วนให้ใช้เต็มความกว้าง

**ตัวเลขต้องโตตามด้วย** ยอดคงเหลือ 38px บนมือถือ ควรเป็น 48–56px บน desktop ไม่ใช่คงที่ 38px

### 5A.3 Compare Mode บนมือถือ

ปัญหา: กรอกข้อเสนอหนึ่งธนาคารมีราว 40–50 ช่อง คูณ 3 ธนาคาร

**สี่วิธีลดจำนวนช่องที่ต้องกรอก**

1. **Preset เติมให้ก่อน** เลือกธนาคารแล้วดึงโครงค่าธรรมเนียมและ MRR จาก `banks` มาเติมอัตโนมัติ ผู้ใช้แค่ยืนยันหรือแก้ ตัดไปได้ราวครึ่งหนึ่ง
2. **กรอกเรตเป็น "ปีที่"** ไม่ใช่ `from_month`/`to_month` ช่อง `ปีที่ 1 [2.50] %` สามแถวพอ แล้วให้ engine แปลงเป็นเดือนเอง เก็บ from_month/to_month ไว้ในโหมดขั้นสูงสำหรับสัญญาที่ไม่ลงตัวเป็นปี
3. **Wizard 5 ขั้น** ธนาคารและวงเงิน → เรต → ค่าธรรมเนียม → ประกัน → ตรวจทาน แต่ละขั้นจบในหนึ่งจอ ไม่ต้องเลื่อนยาว
4. **ให้ผลลัพธ์ตั้งแต่ยังกรอกไม่ครบ** กรอก 6 ช่องแรก (ธนาคาร วงเงิน ระยะเวลา เรต 3 ปีแรก ค่างวด MRTA) ก็คำนวณได้แล้ว แสดงแถบ **ความแม่นยำ** พร้อมรายการที่ยังขาด เช่น "ยังไม่ได้ใส่ค่าจดจำนอง อาจคลาดเคลื่อนถึง 30,000"

หน้าผลลัพธ์ใช้รายการจัดอันดับแบบเดียวกับที่ออกแบบไว้แล้ว ใช้งานบนมือถือได้ดีอยู่แล้ว ไม่ต้องแก้

**บน desktop** เปลี่ยนเป็นคอลัมน์ต่อธนาคารเรียงข้างกัน เห็นทุกช่องพร้อมกัน เลื่อนด้วย Tab และ Enter แบบสเปรดชีต ไม่ใช้ wizard

### 5A.4 Reconciliation บนมือถือ

**หลักการ: ไล่จากปีลงไปหางวด ไม่ใช่ไล่ทีละงวดตั้งแต่ต้น**

เหตุผลเชิงตัวเลข `[MEASURED]` จากสัญญาตัวอย่างในข้อ 9:

| ปี | ผลต่างระหว่าง ACT/365F กับ ACT/ACT |
|---|---|
| 2027 (ปีปกติ) | **0.00** |
| 2028 (ปีอธิกสุรทิน) | **283.17** |
| 2029 | 41.66 |

ในปีปกติวิธีนับวันสองแบบให้ผลเท่ากันทุกสตางค์ การไล่ตรวจ 36 งวดของปีปกติจึงเสียเวลาเปล่า

**สิ่งที่ผู้ใช้ต้องกรอก ลดเหลือปีละตัวเลขเดียว**

ธนาคารออกหนังสือรับรองดอกเบี้ยปีละครั้งสำหรับยื่นภาษีอยู่แล้ว ใช้ตัวเลขนั้นเป็น input หลัก
แทนที่จะให้กรอก 3 ตัวเลข × 36 งวด = 108 ช่อง เหลือ 3 ช่องสำหรับ 3 ปี

**ทางเลือกที่สอง: กระทบยอดด้วยยอดคงเหลืออย่างเดียว** ถ้ายอดคงเหลือตรง แปลว่าการแบ่งต้นกับดอกตรงด้วย เพราะยอดคงเหลือเป็นผลลัพธ์สะสมของการแบ่งทุกงวด ลดจาก 3 ตัวเลขเหลือ 1 ตัวเลขต่องวด

**กางดูรายงวดเฉพาะปีที่ไม่ตรง** แล้วให้ระบบบอกรูปแบบที่พบเป็นภาษาคน ไม่ใช่แค่โชว์ตัวเลขต่าง

ตัวอย่างข้อความที่ระบบควรสรุปให้ `[MEASURED]`:
```
งวด 33 (ม.ค. 2571)  8,092.17 vs 8,092.17   ตรง
งวด 34 (ก.พ. 2571)  8,038.32 vs 8,016.36   +21.96
งวด 35 (มี.ค. 2571) 7,467.69 vs 7,447.22   +20.47
งวด 36 (เม.ย. 2571) 7,926.87 vs 7,905.07   +21.79
อีก 8 งวด ต่างงวดละ 20–27 บาท

พบรูปแบบ: งวด 33 ตรงพอดี แต่งวด 34 เป็นต้นไปต่างสม่ำเสมอ
เพราะงวด 33 คิดดอกจากช่วงเดือน ธ.ค. 2570 ซึ่งยังเป็นปีปกติ
ข้อสันนิษฐาน: ธนาคารใช้ฐาน 366 วันในปีอธิกสุรทิน
ถ้าเปลี่ยนเป็น ACT/ACT ตั้งแต่ 1 ม.ค. 2571 ส่วนต่างเหลือ 0.00
[ไม่ใช่]  [ใช้ค่านี้ตั้งแต่ ม.ค. 2571]
```

การยืนยันต้องสร้างแถวใหม่ใน `loan_conventions` ที่ `effective_from = 2028-01-01` ไม่ใช่แก้แถวเดิม
(สังเกตว่าข้อความที่ผู้ใช้เห็นเป็น พ.ศ. แต่ค่าที่เขียนลง DB เป็น ISO ค.ศ. — นี่คือกฎในข้อ 5.3)

**บน desktop** แสดงตารางเต็มทุกงวด คอลัมน์ `เราคำนวณ | ธนาคารแจ้ง | Δ` พร้อมกันทั้งหมด ไม่ต้องกาง

### 5A.5 ข้อกำหนดทางเทคนิคสำหรับมือถือ

| รายการ | ข้อกำหนด | เหตุผล |
|---|---|---|
| พื้นที่แตะ | ขั้นต่ำ 44 × 44 px | ชิปในภาพร่างสูงราว 30px ซึ่งเล็กเกินไป |
| `font-size` ของช่องกรอก | **ขั้นต่ำ 16px** | ถ้าต่ำกว่านี้ Safari บน iOS จะซูมหน้าจอเองทุกครั้งที่แตะช่องกรอก ทดสอบบน desktop ไม่เจอบั๊กนี้ |
| คีย์บอร์ดตัวเลข | `inputMode="decimal"` | `type="number"` มีปัญหาลูกศรขึ้นลงและค่าเปลี่ยนเมื่อเลื่อนเมาส์ |
| ขอบล่าง | `padding-bottom: env(safe-area-inset-bottom)` | ไม่ให้ปุ่มโดนแถบ home indicator ของ iPhone บัง |
| ติดตั้ง PWA บน iOS | ต้องมีหน้าจอแนะนำ | Safari ไม่มี install prompt ผู้ใช้ต้องกด Share แล้วเลือก Add to Home Screen เอง |
| บันทึกการจ่ายตอนออฟไลน์ | คิวเขียนใน IndexedDB + optimistic UI | อาจบันทึกตอนอยู่ที่เคาน์เตอร์ธนาคารซึ่งสัญญาณไม่ดี |
| ตัวเลขยาว | `font-variant-numeric: tabular-nums` ทุกที่ | คอลัมน์ตัวเลขต้องตรงกัน |

#### Hosting และ deploy

**Cloudflare Workers พร้อม static assets** ตัว worker เดียวทำสองหน้าที่:

1. **serve SPA** — `assets.not_found_handling = "single-page-application"` เพื่อให้ client-side routing ทำงาน
2. **cron trigger** — ping Supabase กัน project pause (ดูตารางข้างล่าง) ไม่ต้องมี service แยก

**Auto deploy:** push ขึ้น `main` → GitHub Actions รัน test แล้ว deploy ด้วย `wrangler`
**กฎ: ถ้า `vitest` ไม่ผ่าน ห้าม deploy** — engine ที่คำนวณดอกเบี้ยผิดแล้วขึ้น production คือสิ่งที่แอพนี้มีไว้เพื่อป้องกัน

```
.github/workflows/deploy.yml
  on: push to main
  1. npm ci
  2. npm run typecheck
  3. npm test          <- TV-1..TV-33 ต้องผ่านครบ
  4. npm run build
  5. wrangler deploy   <- ต้องมี secret CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID
```

PR ที่ยังไม่ merge ให้รันแค่ขั้น 1-4 ไม่ deploy

#### ข้อจำกัดของ Supabase free tier — กระทบสถาปัตยกรรมจริง

ยืนยันแล้วว่าโปรเจกต์นี้จะใช้ free tier พร้อม auto-awake เหมือนแอพอื่นที่ใช้อยู่ ต้องออกแบบรับข้อจำกัดเหล่านี้ตั้งแต่ M2:

| ข้อจำกัด | กระทบอะไร | รับมือ |
|---|---|---|
| **โปรเจกต์ pause หลังไม่มี request 7 วัน** | แอพติดตามสินเชื่อเปิดเดือนละครั้ง จะโดน pause แน่นอน | **Cloudflare Cron Trigger** ping Supabase ทุก 3 วัน — worker ตัวเดียวกับที่ serve แอพ ห้ามพึ่ง `pg_cron` เพราะโปรเจกต์ที่ pause อยู่ cron ก็ไม่รัน |
| **โปรเจกต์ที่ pause ค้างไว้นานอาจถูกลบ** | ⛔ นี่คือความเสี่ยงที่แท้จริง — ข้อมูล 30 ปีหายทั้งชุด | **export อัตโนมัติต้องอยู่ใน v1 ไม่ใช่ future feature** |
| 500MB database | ไม่เป็นปัญหา สัญญาหนึ่งมี ~360 แถว | — |
| ไม่มี PITR / backup ในแผนฟรี | กู้คืนเองไม่ได้ | เหมือนข้างบน |

**ผลที่ตามมา: ต้องเลื่อน export เข้ามาใน v1**

ข้อ 12.2 เดิมจัด "ส่งออกข้อมูลสำหรับยื่นภาษี" เป็น future feature — เมื่อ backend เป็น free tier ที่โปรเจกต์อาจถูกลบ การ export ไม่ใช่ความสะดวกอีกต่อไป แต่เป็น**ความทนทานของข้อมูล**

```
M8 ต้องมี:
  - ปุ่ม export ทั้งบัญชีเป็น JSON ไฟล์เดียว (ทุกทรัพย์สิน ทุกสัญญา ทุกรายการจ่าย)
  - import กลับจากไฟล์นั้นได้ ต้อง round-trip ได้จริง มี test
  - เก็บสำเนาล่าสุดไว้ใน IndexedDB ด้วย เพื่อให้เปิดอ่านได้แม้ backend ล่ม
```

ยึดตาม stack เดิมของโปรเจกต์ PWA ที่มีอยู่:

```
React 18 + TypeScript + Vite
Tailwind CSS (config token ตามข้อ 5.2)
Supabase (Postgres + Auth + RLS)
Cloudflare Workers (static assets) + auto deploy จาก git push
vite-plugin-pwa
```

เพิ่มเติม:
- **เงิน: เก็บเป็น integer สตางค์ (bigint) ทุกที่** ห้ามใช้ float
  ทำ type `type Satang = number & { __brand: 'satang' }` + helper `toSatang/fromSatang/formatTHB`
  (เคยเจอบั๊ก double-counting ใน nutrition app มาแล้ว — money math ต้อง exact)
- **อัตราดอกเบี้ย: เก็บเป็น bps (integer)** เช่น 250 = 2.50%
- กราฟ: **Recharts** สำหรับกราฟมาตรฐาน + **SVG เขียนเอง** สำหรับ Split Ribbon (Recharts ทำริบบิ้น 360 แท่งบนมือถือได้ไม่สวย)
- ตาราง: `@tanstack/react-table` + `@tanstack/react-virtual`
- Engine เป็น **pure TypeScript module ไม่ผูก React** อยู่ที่ `src/engine/` เพื่อให้ unit test ได้ 100%

---

## 7. Data Model (Supabase)

**หลายสินเชื่อต่อผู้ใช้หนึ่งคน** — ตัดสินใจแล้วว่าต้องรองรับ คนหนึ่งอาจมีบ้านหลายหลัง
`properties` เป็น 1-to-many กับ `user_id` อยู่แล้วจึงรองรับเชิงโครงสร้าง แต่มี 3 อย่างที่ต้องทำเพิ่ม ไม่ใช่แค่เพิ่มแถว:

1. **ตัวสลับทรัพย์สิน** ทุกหน้าที่ผูกกับสัญญาต้องรู้ว่ากำลังดูหลังไหน + จำค่าล่าสุดไว้ใน `user_prefs`
2. **Dashboard รวมทุกหลัง** — หนี้รวม ดอกเบี้ยรวมปีนี้ และโซ่รีไฟแนนซ์ของแต่ละหลัง
3. **เพดานลดหย่อนภาษีต้องรวมข้ามสัญญา** ดูคำเตือนในข้อ 1.9 — นี่คือจุดที่พังเงียบที่สุด

```sql
-- ทรัพย์สิน
properties (id, user_id, name, purchase_price_satang, down_payment_satang, created_at)

-- ข้อเสนอจากธนาคาร (ใช้ตอนเปรียบเทียบ)
loan_offers (
  id, property_id, bank_code, bank_name, product_name,
  loan_amount_satang, term_months,
  installment_basis,            -- 'quoted' | 'computed_from_rate'
  installment_quoted_satang,
  lockin_months,                -- default 36
  prepay_penalty_bps,           -- default 300 = 3.00% (bps integer ตามกฎห้ามใช้ float)
  mrta_premium_satang, mrta_financed, mrta_rate_discount_bps,
  day_count_basis,              -- 'ACT/365F' | 'ACT/ACT' | 'ACT/365_SKIP'
  interest_rounding,            -- 'floor_baht' | 'floor_satang' | 'round_satang' | 'none'
  quote_date, source_url, notes
)

-- preset ธนาคาร (seed ตามข้อ 2.5)
banks (bank_code pk, name_th, name_en, is_sfi boolean, rate_notice_url)

offer_rate_steps (id, offer_id, from_month, to_month, kind, fixed_rate_bps, index_code, spread_bps)

offer_fees (
  id, offer_id, fee_type, basis,         -- 'flat' | 'pct_of_loan'
  amount_satang, pct_bps, cap_satang,
  is_waived, waiver_cap_satang, is_financed, clawback_months, note
)

offer_insurance (
  id, offer_id, kind,                    -- 'fire' | 'MRTA' | 'MLTA'
  premium_satang, term_years, coverage_years,
  waived_first_n_years, escalation_bps_per_renewal,
  financed, rate_discount_bps, is_required, surrender_value_bps
)

-- time series อัตราอ้างอิง  แถวเดิมห้ามแก้ ถ้าผิดให้สร้างแถวใหม่แล้วชี้ superseded_by
-- ✅ ตัดสินใจแล้ว: รายผู้ใช้ ไม่ใช่ตารางกลาง  ข้อมูลนี้เป็นของส่วนตัว และแอพมี login อยู่แล้ว
reference_rates (
  id, user_id,                           -- RLS filter ด้วย user_id ตรง ๆ ไม่ผ่าน properties
  bank_code, index_code, rate_bps, effective_date,
  source,                                -- 'manual' | 'bank_notice'
  confidence,                            -- 'confirmed' | 'unconfirmed'
  source_url, entered_at, superseded_by uuid null
)
  -- ผลที่ตามมา: ถ้าจะทำ 12.1 (ดึงจาก ธปท.) ในอนาคต ต้องเพิ่มตารางกลางแยกต่างหาก
  -- แล้วให้ reference_rates ของผู้ใช้ override ค่ากลาง ห้ามเขียนทับตารางผู้ใช้โดยตรง

-- แก้วันตัดเฉพาะงวด  ดูข้อ 1.4.3  override ชนะกฎอัตโนมัติเสมอ
loan_schedule_overrides (
  id, loan_id, period_index int,        -- unique (loan_id, period_index)
  due_date date,
  reason text,                          -- 'user_correction' | 'from_statement' | 'special_holiday'
  created_at
)
  -- override งวดหนึ่งห้ามทำให้งวดถัดไปขยับ  nominal(n) นับจากวันเริ่มสัญญาเสมอ
  -- validate: actual(n-1) < due_date < nominal(n+1)  ไม่งั้น accrualDays ติดลบ

-- ปฏิทินวันหยุดธนาคาร  จำเป็นเมื่อ roll_calendar = 'weekend_and_bank_holidays'
bank_holidays (holiday_date date, name_th, source, year, user_id null)
  -- user_id = null คือวันหยุดที่ seed มาให้ / ไม่ null คือผู้ใช้เพิ่มเอง
  -- ปีที่ยังไม่มีข้อมูล engine fallback เป็นเสาร์-อาทิตย์ แล้วติดป้ายว่าเป็นวันประมาณการ

-- สัญญาที่เซ็นแล้ว
active_loans (
  id, property_id, offer_id, contract_date, first_due_date, due_day_of_month,
  first_accrual_date,           -- ผู้ใช้แก้ได้ default = วันเบิกเงินกู้  ดูข้อ 1.4.2
  date_roll,                    -- 'none' | 'preceding' | 'following'     ดูข้อ 1.4.1
  roll_calendar,                -- 'weekend_only' | 'weekend_and_bank_holidays'
  disbursed_amount_satang, prepay_mode, status
)

-- การจ่ายจริง (ผู้ใช้กรอกแค่ตารางนี้)
payments (
  id uuid pk,                  -- ต้องสร้างฝั่ง client (crypto.randomUUID) ไม่ใช่ default gen_random_uuid()
  loan_id, paid_date, amount_satang, kind, channel, note,
  created_at, deleted_at timestamptz null   -- soft delete เท่านั้น ห้าม DELETE จริง
)
  -- kind: 'installment' | 'partial_prepay' | 'full_redemption' | 'fee'
  -- 5A.5 ระบุ offline queue ใน IndexedDB + optimistic UI -> retry จะสร้างรายการจ่ายซ้ำ
  -- id จึงต้องสร้างฝั่ง client ตั้งแต่ตอน enqueue แล้ว upsert on conflict do nothing
  -- ห้ามลบจริง เพราะงวดที่เคยกระทบยอด 'เขียว' จะเปลี่ยนเงียบโดยไม่มีร่องรอย

-- ผลการคำนวณ (derived, cache ได้ แต่ต้อง recompute ได้เสมอ)
payment_allocations (
  id, payment_id, interest_satang, principal_satang, fee_satang,
  accrued_interest_after_satang,     -- ดอกค้างยกไป ดูข้อ 1.2 (ถ้าเลือกโหมดไม่ทบต้น)
  balance_after_satang, accrual_days, effective_rate_bps, computed_at, engine_version
)
  -- กฎ invalidate: การ insert / แก้ / soft-delete payment ที่ paid_date = D
  -- ต้อง recompute allocation ของทุกงวดตั้งแต่ D เป็นต้นไป ไม่ใช่แค่งวดนั้น
  -- และต้อง re-flag reconciliation ของงวดที่กระทบด้วย ห้ามปล่อยค้างสถานะเดิม

-- ใบแจ้งยอดธนาคาร สำหรับกระทบยอด
statement_entries (id, loan_id, stmt_date, interest_satang, principal_satang, balance_satang, source)

-- เหตุการณ์อัตราเปลี่ยนที่กระทบสัญญานี้
rate_change_events (id, loan_id, effective_date, new_rate_bps, reason, source_url)

-- แผนโปะ / what-if
scenarios (id, loan_id, name, created_at)

prepay_plans (
  id, scenario_id, base_year,
  repeat_mode,                 -- 'single_year' | 'repeat_forever' | 'repeat_until'
  repeat_until_year
)
prepay_months    (id, plan_id, month, amount_satang)              -- 12 แถวของ base_year
prepay_overrides (id, plan_id, year, month, amount_satang)        -- เฉพาะปีที่ต่างจาก base
prepay_lumps     (id, plan_id, pay_date, amount_satang, label)    -- ก้อนเดี่ยวตามวันที่

-- วิธีนับวัน/ปัดเศษ ต้องเป็น effective-dated เพราะธนาคารเปลี่ยน convention กลางสัญญาได้
loan_conventions (
  id, loan_id, effective_from,
  day_count_basis, interest_rounding,
  source,                      -- 'contract' | 'inferred' | 'user_override'
  confidence, note
)

user_prefs (
  user_id pk,
  prepay_view,                 -- 'calendar' | 'list'        default 'calendar'
  schedule_axis,               -- 'contract_year' | 'calendar_year'  default 'contract_year'
  marginal_tax_rate_bps        -- null = ซ่อนคอลัมน์ ROI หลังภาษี (bps เช่น 2000 = 20%)
)
```

**กฎชนิดข้อมูล:** เงิน = `bigint` สตางค์ / อัตราและเปอร์เซ็นต์ทุกตัว = `int` bps — **ห้ามมี column ลงท้าย `_pct` ที่เป็น float ในระบบ** ข้อ 5A.5 ห้าม float กับเงินไว้แล้ว แต่ต้องครอบคลุมเปอร์เซ็นต์ด้วย ไม่งั้น float จะรั่วกลับเข้ามาทางค่าปรับไถ่ถอนและอัตราภาษี

RLS: ตารางของผู้ใช้ filter ด้วย `user_id` ผ่าน `properties` — `payment_allocations` เป็น derived อย่าให้ client เขียนตรง
`reference_rates` filter ด้วย `user_id` ตรง ๆ (ไม่ผ่าน `properties` เพราะอัตราอ้างอิงไม่ผูกกับทรัพย์สินใดทรัพย์สินหนึ่ง)
ข้อยกเว้นเดียว: `banks` เป็น preset อ่านอย่างเดียว (SELECT ได้ทุกคน เขียนได้เฉพาะ service_role)

**✅ ตัดสินใจแล้ว: ไม่ทำกู้ร่วมแบบแชร์บัญชีใน v1** — ไม่มี `property_members` `properties.user_id` เป็นเจ้าของคนเดียว

ที่ยัง**ต้องทำ**คือส่วนของภาษี เพราะกู้ร่วมกระทบการคำนวณแม้จะไม่แชร์บัญชี:
`tax_config.is_joint_loan` + `borrower_count` ยังอยู่ (ข้อ 1.9) → หารสิทธิลดหย่อนตามจำนวนผู้กู้ได้ โดยที่คู่กู้ไม่ต้องมี account ในระบบ

ถ้าวันหนึ่งจะทำการแชร์จริง: เพิ่มตาราง `property_members (property_id, user_id, role)` แล้วเปลี่ยน RLS จาก `properties.user_id = auth.uid()` เป็น subquery ผ่านตารางนั้น — ข้อมูลที่มีอยู่ไม่ต้อง migrate เพราะเจ้าของเดิมกลายเป็น member คนแรก

---

## 8. Engine API

**กฎวันที่ของ engine — ห้ามละเมิด**

ทั้ง engine อ่อนไหวต่อ "จำนวนวัน" โดยตรง ถ้าใช้ `Date` ของ JavaScript ซึ่งมี timezone ติดมา การแปลงข้ามเขตเวลาจะเลื่อนวันไป 1 วันโดยไม่มีใครเห็น ดอกเบี้ยจะผิดทั้งงวดทันที และเป็นบั๊กที่ test บนเครื่อง dev ไม่เจอเพราะ tz ตรงกันพอดี

```ts
type ISODate = string & { __brand: 'ISODate' }   // 'YYYY-MM-DD' เท่านั้น ไม่มีเวลา ไม่มี timezone
```

- ภายใน `src/engine/` **ห้าม `new Date()` ห้าม `Date.now()` ห้าม `toISOString()`**
- คำนวณส่วนต่างวันด้วย civil date ล้วน (แปลงเป็น day-number แล้วลบกัน หรือใช้ `Temporal.PlainDate`)
- "วันนี้" ต้องฉีดเข้ามาเป็นพารามิเตอร์เสมอ ห้าม engine อ่านนาฬิกาเอง ไม่งั้น test จะ flaky ตามวันที่รัน
- ตั้ง lint rule `no-restricted-globals: Date` เฉพาะโฟลเดอร์ `src/engine/`

```ts
// src/engine/index.ts — pure, ไม่มี side effect, ไม่ผูก React

type LoanTerms = {
  principalSatang: Satang
  startDate: ISODate                  // = first_accrual_date ดูข้อ 1.4.2
  termMonths: number
  dueDayOfMonth: number
  dateRoll: 'none' | 'preceding' | 'following'
  rollCalendar: 'weekend_only' | 'weekend_and_bank_holidays'
  bankHolidays: ISODate[]             // ฉีดเข้ามา engine ห้ามไปโหลดเอง
  scheduleOverrides: Record<number, ISODate>   // period_index -> วันตัดที่ผู้ใช้กำหนด ดูข้อ 1.4.3
  rateSteps: RateStep[]
  referenceRates: ReferenceRate[]     // history + projection (projection = flat-forward จากค่า confirmed ล่าสุด)
  conventions: LoanConvention[]       // effective-dated เสมอ ห้ามเป็นค่าเดียว ดูข้อ 1.1.1
  installment: { basis: 'quoted'; satang: Satang } | { basis: 'computed' }
}

type DayCountBasis = 'ACT/365F' | 'ACT/ACT' | 'ACT/365_SKIP'
type RoundingMode  = 'floor_baht' | 'floor_satang' | 'round_satang' | 'none'

// ห้ามยุบเป็น field เดียวในสัญญา เพราะธนาคารเปลี่ยน convention กลางสัญญาได้ (ข้อ 1.1.1)
// และโมดูล reconciliation ใน 5A.4 ต้องสร้างแถวใหม่ที่ effectiveFrom = 2028-01-01 ได้
type LoanConvention = {
  effectiveFrom: ISODate              // เรียงจากน้อยไปมาก แถวแรกต้องครอบ startDate
  dayCountBasis: DayCountBasis
  rounding: RoundingMode
}

type PaymentEvent = {
  date: ISODate
  amountSatang: Satang
  kind: 'installment' | 'partial_prepay' | 'full_redemption'
}

type ScheduleRow = {
  index: number
  date: ISODate
  accrualDays: number
  effectiveRateBps: number
  paymentSatang: Satang
  interestSatang: Satang
  principalSatang: Satang
  balanceAfterSatang: Satang
  flags: ('negative_amortization' | 'below_minimum' | 'rate_changed' | 'prepay')[]
}

// core
function buildSchedule(terms: LoanTerms, events: PaymentEvent[]): ScheduleRow[]
function projectSchedule(terms: LoanTerms, fromRow: ScheduleRow): ScheduleRow[]

// compare
function evaluateOffer(offer: LoanOffer, opts: { equalPaymentSatang?: Satang }): OfferMetrics
function rankOffers(offers: LoanOffer[], opts): RankedOffer[]
function sensitivityBand(offer: LoanOffer, deltasBps: number[]): Record<number, OfferMetrics>

// what-if
function simulatePrepay(terms, events, plan: PrepayPlan): { rows, savedInterestSatang, monthsSaved }
function refinanceBreakeven(current, newOffer): { breakevenMonth: number | null, netSaving }

// reconcile
function reconcile(computed: ScheduleRow[], stmt: StatementEntry[]): ReconResult[]
// inference 2 ขั้น ดูข้อ 3.3
function inferDateRule(terms, stmt): { dateRoll, rollCalendar, unmatchedPeriods: number[] }
function inferAmountConventions(terms, events, stmt, dateRule): InferResult[]  // 24 combos เรียงตาม MAE
// คืนทุก combination ที่ MAE ต่างกันไม่ถึง 1 บาท ไม่ใช่ตัวเดียว เพื่อกัน overfit
```

**เวอร์ชัน engine:** เก็บ `engine_version` ใน `payment_allocations` เพื่อให้รู้ว่าแถวไหนคำนวณด้วย logic เก่า และ recompute ได้

---

## 9. Test Vectors — ต้องผ่านก่อนเขียน UI

เขียน Vitest ให้ผ่านทั้งหมดก่อนแตะ component ใด ๆ

### 9.1 สมมติฐานร่วมของทุก TV

ถ้าไม่ตั้งตามนี้จะได้เลขไม่ตรงในทศนิยมแล้วหาสาเหตุไม่เจอว่าเป็นบั๊กของโค้ดหรือของ spec

| พารามิเตอร์ | ค่า |
|---|---|
| `rounding` | **`'none'`** exact decimal — ตัวเลขทุกตัวในเอกสารนี้คำนวณแบบไม่ปัด |
| accrual window | `[วันครบกำหนดงวดก่อนหน้า, วันครบกำหนดงวดนี้)` ปลายเปิด |
| วันครบกำหนดงวดที่ N | วันเดียวกันของเดือนถัดไป clamp ที่วันสุดท้ายของเดือน (ข้อ 1.4.1) |
| `date_roll` | `'none'` ไม่สนวันหยุด |
| งวดแรก | accrue จาก `startDate` ถึงวันครบกำหนดงวดที่ 1 |
| `day_count_basis` | `ACT/365F` เว้นแต่ข้อนั้นระบุเป็นอย่างอื่น |

> ยืนยันแล้วว่าภายใต้สมมติฐานชุดนี้ ค่า `[MEASURED]` ทั้งหมดในเอกสาร reproduce ได้ตรงทุกสตางค์จาก engine ที่เขียนขึ้นใหม่โดยอิสระ

### 9.2 TV-0 — ยังไม่มี ต้องหามาก่อนปิด M1

**TV-1 ถึง TV-27 อ้างอิงตัวเองทั้งหมด** — `[MEASURED]` แปลว่าวัดจากโมเดลในเอกสารนี้เอง ผ่านครบจึงพิสูจน์แค่ว่า engine ตรงกับ spec **ไม่ได้พิสูจน์ว่าตรงกับธนาคารไทยจริง** ซึ่งคือคุณค่าทั้งหมดของแอพตามข้อ 0

```
TV-0  ใบแจ้งยอด / ตารางผ่อน / หนังสือรับรองดอกเบี้ย ของจริง 1 ฉบับ
  input : วงเงิน, วันทำสัญญา, วันเบิกเงินกู้, เรตตามสัญญา, ค่างวด, รายการจ่ายจริง
  expect: ดอกเบี้ย / เงินต้น / ยอดคงเหลือ ตรงกับที่ธนาคารแจ้งทุกงวด  |Δ| ≤ 1 บาท
```

ใบเดียวปิดคำถามที่ค้างอยู่พร้อมกันหมด:
งวดแรก accrue จากวันไหน (1.4.2) · ปัดเศษที่จุดไหน (3.3) · เลื่อนวันหยุดหรือไม่ (1.4.1) · ดอกค้างทบต้นไหม (1.2) · default `ACT/365F` ถูกไหม (1.1.1)

### 9.3 Test vectors

```
TV-1  ดอกเบี้ยรายวัน
  P=3,000,000.00  rate=3.00%  days=31
  → interest = 7,643.84  (7,643.8356 ปัดตาม rounding mode)
  [MEASURED: 3000000 × 0.03 / 365 × 31]

TV-2  การแบ่งงวด
  ต่อจาก TV-1, payment = 16,000.00
  → interest 7,643.84 / principal 8,356.16 / balance 2,991,643.84

TV-3  Negative amortization guard — ต้องทดสอบทั้งสองโหมด (ดูข้อ 1.2)
  P=3,000,000  rate=7.00%  days=31  payment=17,000
  → interest 17,835.62 > payment → flag 'negative_amortization' ทั้งสองโหมด
  [MEASURED: 3000000 × 0.07 / 365 × 31 = 17,835.6164]

  capitalise_unpaid_interest = false  (default)
    → balance คงที่ 3,000,000.00   accruedInterestCarried = 835.62
  capitalise_unpaid_interest = true
    → balance เพิ่มเป็น 3,000,835.62   accruedInterestCarried = 0

  เดินต่ออีก 11 งวดที่ค่างวด 17,000 (รวม 12 งวด) หนี้รวม (balance + accrued) ต้องเป็น
    false → 3,005,996.78
    true  → 3,006,163.78     ต่างกัน 167.00 เพราะฝั่ง true คิดดอกจากดอก
  [MEASURED]

TV-4  PMT reference
  P=3,000,000  n=360  rate=6.00%/yr
  → PMT ≈ 17,987 (ใช้เช็คคร่าว ๆ เท่านั้น ตารางจริงต้องต่างจากนี้)

TV-5  เดือนสั้น/ยาว
  งวด ก.พ. (28 วัน) ต้องคิดดอกน้อยกว่างวด ม.ค. (31 วัน) ที่เงินต้นเท่ากัน
  ผลต่างต้องเท่ากับ balance × rate / 365 × 3 พอดี

TV-6  Rate step transition
  โปร 2 ปี fixed 2.50% → ปีที่ 3 MRR−1.75% (MRR=7.00% → 5.25%)
  งวดที่ 24 ต้องใช้ 2.50%, งวดที่ 25 ต้องใช้ 5.25%, flag 'rate_changed'

TV-7  Reference rate เปลี่ยนกลางงวด
  MRR เปลี่ยนวันที่ 15 ของงวด → ดอกเบี้ยงวดนั้น = (14 วัน @ อัตราเก่า) + (N วัน @ อัตราใหม่)
  ห้ามใช้อัตราเดียวทั้งงวด

TV-8  โปะ shorten_term
  โปะ 100,000 ที่งวด 12 → ค่างวดเท่าเดิม จำนวนงวดลดลง ดอกเบี้ยรวมลดลง
  ตรวจว่า monthsSaved > 0 และ balance ที่งวดสุดท้าย = 0 (ไม่ติดลบ)

TV-9  โปะ reduce_installment
  โปะก้อนเดียวกัน → จำนวนงวดเท่าเดิม ค่างวดลดลง
  ต้องได้ savedInterest น้อยกว่า TV-8

TV-10 งวดสุดท้าย
  งวดสุดท้ายต้องเป็น balance คงเหลือ + ดอกเบี้ย ไม่ใช่ค่างวดเต็ม
  balance หลังจ่าย = 0 พอดี (ไม่ใช่ 0.01 หรือ −0.03)

TV-11 Equal-payment comparison
  2 ข้อเสนอ ค่างวดล็อก 20,000 เท่ากัน
  Net Position @ M36 ต้องคำนวณได้ และธนาคารที่ค่าธรรมเนียมสูงกว่าแต่ดอกถูกกว่า
  ต้องแพ้/ชนะตามผลรวม ไม่ใช่ตามดอกเบี้ยอย่างเดียว

TV-12 Reconciliation inference
  ป้อน statement 12 งวดที่สร้างจาก basis=ACT/365F + rounding=floor_baht
  → inferBankConventions ต้องคืนค่าถูกต้อง MAE < 1 บาท

TV-13 Day-count: ปีปกติต้องแยกไม่ออก
  รัน ACT/365F กับ ACT/ACT บนช่วง 1 ม.ค. 2027 → 1 ม.ค. 2028 (ปีปกติ)
  → ผลต้อง "เท่ากันทุกสตางค์"
  ถ้าต่างกัน แปลว่า implement ผิด

TV-14 Day-count: ก.พ. ปีอธิกสุรทิน
  P=2,900,000  rate=5.00%  ช่วง 2028-02-01 → 2028-03-01 (29 วัน)
  → ACT/365F     = 11,520.55
  → ACT/ACT      = 11,489.07
  → ACT/365_SKIP = 11,123.29
  [MEASURED]

TV-15 Day-count: ตัดที่ขอบปี
  ACT/ACT, ช่วง 2027-12-15 → 2028-01-15 (31 วัน)
  → ต้องเป็น (17 วัน ÷ 365) + (14 วัน ÷ 366)  ไม่ใช่ (31 ÷ 366) หรือ (31 ÷ 365)

TV-16 Day-count: ผลรวมตลอดสัญญา
  P=3,000,000  เริ่ม 2026-10-01  คงที่ 5.00%  ค่างวด 16,200
  → ACT/365F ดอกเบี้ยรวม 2,758,560.20 (356 งวด)
  → ACT/ACT  ดอกเบี้ยรวม 2,753,376.69 (356 งวด)
  [MEASURED]

TV-17 MRTA รวมในวงเงิน
  ฐาน 3,000,000 @5.00% ค่างวด 16,200 → 356 งวด ดอกเบี้ยรวม 2,758,560.20
  รวม MRTA 100,000 → 3,100,000 ค่างวดเท่าเดิม → 385 งวด
  ต้นทุนรวมของเบี้ย 100,000 = 466,684.80  [MEASURED]
  ทดสอบซ้ำที่ค่างวด 20,000 → ต้องได้ 275,268 (±1)

TV-18 กับดักยืดเทอมตอนรีไฟแนนซ์
  ยอดคงเหลือ 2,624,036.54 ณ 2029-10-01, โปรใหม่ 3 ปี 3.00% → 5.50%
  → คงค่างวด 17,500  : ดอกเบี้ยต่อจากนี้ 1,322,382  (226 งวด)
  → ลดค่างวด 15,000  : ดอกเบี้ยต่อจากนี้ 1,920,701  (303 งวด)
  → ไม่รีไฟแนนซ์ 5.50% ค่างวด 17,500 : 1,826,714  (255 งวด)
  engine ต้อง flag ว่าเคส "ลดค่างวด" แพงกว่า "ไม่ทำอะไร" 93,987 บาท
  [MEASURED]

TV-19 ค่าธรรมเนียมแบบมีเพดาน
  อากรแสตมป์ 0.05% เพดาน 10,000
    วงเงิน 15,000,000 → 7,500   ยังไม่ชนเพดาน จ่าย 7,500
    วงเงิน 25,000,000 → 12,500  ชนเพดาน จ่าย 10,000
  ค่าจดจำนอง 1% ของ 15,000,000 = 150,000 แต่ "ฟรีสูงสุด 100,000"
  → ผู้กู้ยังต้องจ่ายเอง 50,000

TV-20 ประกันอัคคีภัยแบบเกิดซ้ำ
  เบี้ย 3,000 บาท/3 ปี, อายุสัญญา 30 ปี, ฟรี 3 ปีแรก
  → ต้องมี cash outflow 9 ครั้ง (ปีที่ 4,7,10,...,28) ไม่ใช่ 10 ครั้ง
  → ต้องเข้า TCO และเข้า XIRR ที่เวลาจริง ไม่ใช่รวมเป็นก้อนที่ t0

TV-21 แผนโปะไม่เท่ากันรายเดือน
  P=3,000,000 เริ่ม 2025-04-01 ค่างวด 20,000
  เรต: งวด 1-12 = 2.50%, 13-24 = 3.25%, 25-36 = 3.75%, 37+ = 4.83%
  แผนโปะเริ่มงวด 21 ซ้ำทุกปี:
    ม.ค. 5,000 / ก.พ. 5,500 / มี.ค. 5,000 / เม.ย.-พ.ย. 4,000 / ธ.ค. 0
  → รวม 170 งวด ปิด 2039-06-01 ดอกเบี้ยรวม 990,217
  [MEASURED]

TV-22 การ resolve ยอดโปะ
  override ต้องชนะ prepay_months
  prepay_lumps ต้อง "บวกเพิ่ม" ไม่ใช่ "แทนที่"
  ถ้ากฎซ้อนกันในเดือนเดียว ต้องบวกรวมทั้งหมด

TV-23 guard ยอดโปะเกินหนี้
  โปะก้อนที่มากกว่ายอดคงเหลือ + ดอกค้าง
  → ตัดพอดีที่ยอดคงเหลือ balance = 0 ห้ามติดลบ ห้ามคืนเงินทอน

TV-24 ปีสัญญา: เรตที่จ่ายจริงต้องตรงกับที่ตกลง
  จาก TV-21 จัดกลุ่มแบบ contract_year
  → ปี 1 = 2.50%, ปี 2 = 3.25%, ปี 3 = 3.75%, ปี 4 ขึ้นไป = 4.83%
  คำนวณด้วย Σดอกเบี้ย ÷ (Σ(เงินต้นก่อนจ่าย × วัน) ÷ 365)
  ดอกเบี้ยรายปีสัญญา: 73,104 / 89,779 / 96,910 / 115,708  [MEASURED]

TV-25 สองแกนต้องรวมได้เท่ากัน
  groupSchedule(rows,'contract_year') กับ groupSchedule(rows,'calendar_year')
  ผลรวมดอกเบี้ย เงินต้น และยอดจ่าย ต้องเท่ากันทุกสตางค์
  จำนวนงวดรวมต้องเท่ากัน

TV-26 ปีปฏิทิน: เพดานลดหย่อน
  จาก TV-21 จัดกลุ่มแบบ calendar_year
  → 2025 มี 8 งวด ดอก 49,330  (ต้องติดป้ายว่าเป็นปีไม่เต็ม)
  → 2028 ดอก 109,770 เกินเพดาน 9,770
  → 2029 ดอก 110,082 เกินเพดาน 10,082
  → 2030 ดอก 101,321 เกินเพดาน 1,321
  รวมส่วนที่ใช้สิทธิไม่ได้ 21,173  [MEASURED]

TV-27 ห้ามเฉลี่ยเรตแบบเลขคณิต
  ตรวจโฆษณา "เฉลี่ย 3 ปี" ของโครงสร้าง 2.50 / 3.25 / 3.75
  → ค่าเฉลี่ยเลขคณิต = 3.17%  (ตัวเลขที่ธนาคารโฆษณา)
  → เรตที่จ่ายจริง 36 งวดถ่วงน้ำหนัก = 3.14%  ดอกเบี้ยรวม 259,793
  engine ต้องคืนค่า 3.14% ไม่ใช่ 3.17%
  [MEASURED]

TV-28 Invariant: ห้ามมีสตางค์หาย  (property-based ไม่ใช่ค่าคงที่)
  รันด้วย input สุ่ม 1,000 ชุด (วงเงิน / เรต / ค่างวด / วันเริ่ม / แผนโปะ)
  ทุกชุดต้องผ่านทั้ง 4 ข้อ:
    a) Σ payment = Σ interest + Σ principal + Σ fee                  ทุกสตางค์
    b) Σ principal = วงเงินตั้งต้น (รวมรายการ financed)               ทุกสตางค์
    c) balance ลดลงอย่างเดียว เว้นงวดที่ flag negative_amortization
    d) balance งวดสุดท้าย = 0 พอดี ไม่ใช่ 0.01 และไม่ใช่ -0.03
  TV แบบค่าคงที่ 27 ข้อจับบั๊กได้ทีละเคส ข้อนี้จับได้ทั้งคลาส

TV-29 date_roll — ทิศทางต้องถูก
  P=3,000,000 @5.00% ค่างวด 16,200 ตัดทุกวันที่ 5 เริ่ม 2026-10-05
  ปฏิทินวันหยุด = เสาร์-อาทิตย์ + วันหยุดธนาคารแบบวันที่ตายตัว
  → 'none'       356 งวด  ดอกรวม 2,758,560.20   งวดที่ขยับ 0
  → 'preceding'  356 งวด  ดอกรวม 2,757,632.62   งวดที่ขยับ 122  (ดอก "ลดลง")
  → 'following'  356 งวด  ดอกรวม 2,759,468.07   งวดที่ขยับ 122  (ดอก "เพิ่มขึ้น")
  ทดสอบทิศทางให้ชัด: preceding ต้องได้ดอกน้อยกว่า none เสมอ ห้ามสลับเครื่องหมาย
  [MEASURED]

TV-30 เพดานลดหย่อนภาษีต้องรวมข้ามสัญญา
  ผู้ใช้หนึ่งคน มี 2 สัญญา ปีภาษีเดียวกัน ดอกเบี้ย 85,000 และ 60,000
  → สิทธิลดหย่อนที่ใช้ได้ต้องเป็น 100,000 ไม่ใช่ 145,000
  → มิเตอร์ของสัญญา A ต้องรายงาน "มีส่วนร่วม 85,000 จากเพดานรวม 100,000"
     ห้ามรายงานเป็น 85,000/100,000 ของตัวเอง
  ดูข้อ 1.9

TV-31 override วันตัดรายงวด  (ดูข้อ 1.4.3)
  P=3,000,000 @5.00% ตัดวันที่ 5, date_roll='none'
  ตั้ง override งวด 12 จากวันที่ 5 เป็นวันที่ 3
    ก่อน override  งวด 12  5 ต.ค.->5 พ.ย.  31 วัน  12,739.73
                   งวด 13  5 พ.ย.->5 ธ.ค.  30 วัน  12,328.77
    หลัง override  งวด 12  5 ต.ค.->3 พ.ย.  29 วัน  11,917.81
                   งวด 13  3 พ.ย.->5 ธ.ค.  32 วัน  13,150.68
  → ผลรวมวันของสองงวด = 61 ทั้งก่อนและหลัง ห้ามมีวันหายหรือนับซ้ำ
  → งวด 14 เป็นต้นไปกลับมาเป็นวันที่ 5 และ accrualDays ปกติทุกงวด
  ถ้างวด 13 ไม่กลับมาที่วันที่ 5 แปลว่า implement แบบ anchor เลื่อน = ผิด
  [MEASURED]

TV-32 anchor ต้องไม่ไหล  (ดูข้อ 1.4.3)
  ตัดวันที่ 5, date_roll='preceding', ปฏิทิน weekend+bank_holidays, 360 งวด
  → วันตัดที่เกิดขึ้นจริงต้องอยู่ในช่วงวันที่ 2-5 เท่านั้น
  → ห้ามมีงวดไหนตกวันที่ 6 ขึ้นไป หรือวันที่ 1 ลงไป
  ถ้าเจอวันที่นอกช่วงนี้ แปลว่านับ nominal ต่อจาก actual งวดก่อน = ผิด
  [MEASURED: anchor คงที่ได้วันที่ {2,3,4,5} / anchor เลื่อนได้ครบ 1-31]

TV-33 guard ของ override
  ตั้ง override งวด n ให้เร็วกว่าวันตัดของงวด n-1
  → ต้อง reject ที่ชั้น validate ห้ามปล่อยให้ engine คำนวณ
  → ถ้าหลุดเข้า engine ต้อง throw ไม่ใช่คืน accrualDays ติดลบ
```

---

## 10. ลำดับการสร้าง (Milestones)

**M1 — Engine (ไม่มี UI)**
`src/engine/` + Vitest ผ่าน TV-1 ถึง TV-27 ครบ
ห้ามไปต่อถ้ายังไม่ผ่านทุกข้อ
ลำดับภายใน M1:
accrual + day-count (TV-1,3,5,13–16) → allocation (TV-2,4,10) → rate steps (TV-6,7)
→ prepay พื้นฐาน (TV-8,9) → prepay plan resolver (TV-21,22,23)
→ fees/insurance (TV-19,20) → grouping + effective rate (TV-24,25,26,27)
→ date_roll + ปฏิทินวันหยุด (TV-29) → override วันตัด (TV-31,32,33) → ภาษีข้ามสัญญา (TV-30)
→ compare (TV-11,17) → refinance (TV-18) → inference (TV-12) → invariants (TV-28)

**M2 — Schema + Auth + Import**
Supabase migration (รวม `loan_conventions`, `offer_fees` แบบมีเพดาน, `refinance chain`), RLS policies
**รวม flow "import สัญญาที่ผ่อนไปแล้ว" ทั้งแบบ Quick และ Full ตั้งแต่ milestone นี้**
seed script สร้างข้อเสนอตัวอย่าง 3 ธนาคาร (ข้อมูลสมมติ ใส่ label ชัดว่า sample)

**M3 — Compare Mode**
ฟอร์มกรอกข้อเสนอ (rate steps + fees + insurance แบบ repeatable rows), equal-payment toggle, ตารางเปรียบเทียบ, sensitivity band, กราฟ cumulative cost curve, การเทียบ ทำ/ไม่ทำ MRTA

**M3.5 — Refinance Mode**
เทียบ อยู่เฉย ๆ / Retention / Refinance พร้อมกัน, break-even, คำเตือนกับดักยืดเทอม, ช่วงมูลค่าเวนคืน MRTA

**M4 — Track Mode**
บันทึกการจ่าย, ตารางผ่อน 3 layer (Plan / Actual / Statement)
จัดกลุ่มรายปี พร้อม toggle ปีสัญญา กับ ปีปฏิทิน ที่สลับคอลัมน์ขวาด้วย
กางปีเพื่อดูรายเดือน

**M5 — Dashboard**
แผง hero สีหมึกเข้ม, Split Ribbon (SVG เขียนเอง), แถบมิเตอร์สิทธิลดหย่อน, กราฟ 2/3/6

**M6 — Prepay Planner**
มุมมองปฏิทินกับรายการ, การเลือกช่วง, ชิปค่าสำเร็จรูป, คีย์แพดพิมพ์ค่าเอง,
ผลลัพธ์อัปเดตสด, ROI และ ROI หลังภาษี, คัดลอกไปปีถัดไป, เทียบหลาย scenario

**M7 — Reconciliation**
เทียบกับใบแจ้งยอด, inference 2 ขั้น (วันตัด 5 แบบ → จำนวนเงิน 24 แบบ), ปฏิทินวันหยุดธนาคาร, ตรวจจับ drift ต่อเนื่องในปีอธิกสุรทิน

**M8 — PWA + polish + ความทนทานของข้อมูล**
manifest, service worker, offline read, install prompt, ปรับ mobile 380px
**export/import ทั้งบัญชีเป็น JSON** (จำเป็นเพราะ Supabase free tier ดูข้อ 5A.5) + สำเนาใน IndexedDB
ตั้ง external ping กัน project pause

---

## 11. สถานะการตัดสินใจ

### 11.1 ✅ ตัดสินใจแล้ว — ห้ามถามซ้ำ

**เรื่องเดิม**
- รีไฟแนนซ์เป็นโหมดหลัก + import สัญญาเดิม → M2 / M3.5
- day-count 3 โหมด effective-dated default `ACT/365F` ยืนยันด้วย reconciliation ตอนปีอธิกสุรทิน 2571
- ค่าจดจำนอง อากรแสตมป์ ประกันอัคคีภัย MRTA เข้าโมเดลต้นทุนครบ รองรับ "ฟรีแบบมีเพดาน"
- แผนโปะเก็บเป็น 12 เดือน + override รายปี ไม่ใช่ array 360 ช่อง
- ตารางผ่อนมี 2 แกน ปีสัญญาเป็น default และ toggle ต้องสลับคอลัมน์ขวาด้วย
- คู่สี น้ำเงินหมึก / เหลืองส้ม hero ใช้แผงสีหมึกเข้ม
- ดึง MRR อัตโนมัติ = future feature ไม่ทำใน v1

**รอบนี้**

| # | เรื่อง | ผล | อยู่ที่ |
|---|---|---|---|
| 1 | เบิกเงินกู้หลายงวด / ผ่อนดอกอย่างเดียว | **นอกขอบเขต v1** | 0.1 |
| 2 | ปีใน UI | **พ.ศ.** — DB เป็น ISO ค.ศ. เสมอ แปลงที่ชั้น format | 5.3 |
| 3 | ดอกเบี้ยค้างชำระ | **config 2 โหมด** default ไม่ทบต้น + โยนเข้า inference | 1.2 |
| 4 | จุดที่ปัดเศษ | **จบงวด** สะสม exact ระหว่างทาง ไม่ทำโหมดปัดรายวัน | 3.3 |
| 5 | วันครบกำหนดตรงวันหยุด | **config 3 โหมด** `none`/`preceding`/`following` + ต้องมีปฏิทินวันหยุดธนาคาร | 1.4.1 |
| 6 | `reference_rates` | **รายผู้ใช้** filter ด้วย `user_id` ตรง ๆ | 7 |
| 7 | หลายสินเชื่อต่อผู้ใช้ | **ต้องรองรับ** + เพดานลดหย่อนภาษีต้องรวมข้ามสัญญา | 7 / 1.9 |
| 8 | Supabase free tier | **export ทั้งบัญชีต้องอยู่ใน v1** เพราะโปรเจกต์ที่ pause ค้างอาจถูกลบ | 5A.5 |
| 9 | งวดแรก accrue จากวันไหน | **`first_accrual_date` เป็น field ที่ผู้ใช้แก้ได้** default = วันเบิกเงินกู้ | 1.4.2 |
| 10 | วันตัดรายงวด | **override รายงวดได้** กฎอัตโนมัติเป็นแค่ค่าตั้งต้น + anchor ห้ามไหล | 1.4.3 |
| 11 | ปฏิทินวันหยุด | **เป็น option** `weekend_only` (default) หรือ `weekend_and_bank_holidays` | 1.4.3 |
| 12 | Convention inference | **2 ขั้น** — วันตัดจับคู่จากวันที่ (5 แบบ) แล้วค่อย fit จำนวนเงิน (24 แบบ) กัน overfit | 3.3 |

**แนวคิดร่วมของข้อ 3/5/9/10/11:** ทุกเรื่องที่ธนาคารทำไม่เหมือนกัน ให้เป็น **config + ค่า default ที่สมเหตุสมผล + ผู้ใช้แก้ได้ + ให้ inference ช่วยหา** แทนการไล่หากฎที่ถูกต้องสากล — เพราะกฎแบบนั้นไม่มีอยู่จริงในตลาดสินเชื่อไทย

| 13 | กู้ร่วมแบบแชร์บัญชี | **ไม่ทำใน v1** ไม่มี `property_members` — แต่ `tax_config.borrower_count` ยังอยู่เพื่อหารสิทธิลดหย่อน | 7 |

### 11.2 ไม่มีข้อค้างที่บล็อกแล้ว

ทุกอย่างที่บล็อก M1 และ M2 ตัดสินใจครบแล้ว **เริ่มเขียนได้ทันที**

สิ่งเดียวที่ยังควรทำคู่ขนานไปด้วยคือหา **TV-0** (ใบแจ้งยอดจริง 1 ฉบับ) มายืนยัน default ของข้อ 3/4/5/9 ดูข้อ 9.2 — ถ้า default ตัวไหนผิด แก้ที่ config ไม่ต้องแก้ engine

### 11.3 ตอบทีหลังได้ ไม่บล็อก

11. ธนาคารเป้าหมายมีใครบ้าง → เอาไว้เติม fee structure และ day-count basis ที่รู้แล้วลง preset
12. มี statement แบบดาวน์โหลดได้ไหม (PDF/CSV) → ถ้ามี ทำ parser ใน M7 ถ้าไม่มีต้องกรอกมือ

---

## 12. Future features (ไม่ทำใน v1)

### 12.1 ดึง MRR อัตโนมัติจาก BOT API

ค้นข้อมูลไว้แล้ว สรุปสถานะเพื่อไม่ต้องค้นซ้ำตอนจะทำ

**สิ่งที่ยืนยันแล้ว** `[SOURCE: ธปท.]`
- ธปท. เผยแพร่อัตราดอกเบี้ยประจำวันของธนาคารพาณิชย์เป็นรายธนาคาร ที่ `bot.or.th/th/statistics/interest-rate.html`
- มีบริการ API อย่างเป็นทางการ ระบบใหม่ที่ `portal.api.bot.or.th` เปิดใช้ 17 ก.ย. 2568 ระบบเดิมให้บริการถึง 31 ธ.ค. 2568
- กลุ่มข้อมูลสถิติเรียกด้วย `series_code` + `start_period` + `end_period` ยืนยันตัวตนด้วย API key ผ่าน header `X-IBM-Client-Id`
- ข้อมูลที่ให้บริการรวมถึงอัตราดอกเบี้ย

**สิ่งที่ยังไม่ยืนยัน ต้องไปตรวจเอง**
- มี `series_code` ของ MRR **รายธนาคาร** ในแคตตาล็อกหรือไม่ ต้องสมัครสมาชิกแล้วดาวน์โหลด List of Statistics APIs มาดู
- ธอส. กับออมสินเป็นสถาบันการเงินเฉพาะกิจ น่าจะไม่อยู่ในตารางธนาคารพาณิชย์ ธอส. มีหน้าประกาศของตัวเองแยกต่างหาก

**ข้อจำกัดที่แก้ไม่ได้ด้วย API**
MRR เป็นแค่ครึ่งเดียวของสูตร อัตราที่จ่ายจริงคือ `MRR − spread ตามสัญญา` ซึ่ง spread เป็นตัวเลขเฉพาะรายสัญญา ไม่มีที่ไหนเผยแพร่ ต้องกรอกมือตลอดไป

**สถาปัตยกรรมเมื่อจะทำ**
```
Supabase Edge Function + cron รายสัปดาห์
  → เรียก BOT API (API key เก็บฝั่ง server เท่านั้น)
  → INSERT ลง reference_rates  ห้าม UPDATE
  → ถ้าค่าต่างจากแถวล่าสุด สร้าง notification ให้ผู้ใช้ยืนยัน
```
ไม่ต้องดึงรายวัน MRR เปลี่ยนไม่กี่ครั้งต่อปีและมักขยับตามการประชุม กนง.
ดึงจากฝั่ง client ไม่ได้เพราะ API key จะโผล่ในเบราว์เซอร์และติด CORS

**กฎเหล็กที่ต้องยึดตั้งแต่ v1 แม้ยังไม่ทำ feature นี้**
ข้อมูลที่ดึงมาอัตโนมัติห้ามเขียนทับอัตราที่ใช้คำนวณย้อนหลัง เพราะธนาคารอาจปรับอัตราจริงคนละวันกับที่ ธปท. เผยแพร่ ถ้าระบบแก้ย้อนหลังเอง ตารางที่เคยกระทบยอดตรงกับใบแจ้งยอดจะเพี้ยนโดยไม่มีใครรู้
→ ตาราง `reference_rates` ต้อง immutable ตั้งแต่ v1 แก้ค่าเก่าด้วยการสร้างแถวใหม่แล้วชี้ `superseded_by`
→ UI ต้องขึ้นเป็น "ธปท. ประกาศ 6.58% ต่างจากที่บันทึกไว้ 6.50%" พร้อมปุ่มยืนยัน ห้ามเปลี่ยนเงียบ

**ห้ามทำ** scrape เว็บข่าวหรือเว็บรวมอัตราดอกเบี้ย เปราะและเสี่ยงผิดเงื่อนไขการใช้งาน

### 12.2 อื่น ๆ
- Parser ใบแจ้งยอด PDF/CSV ของแต่ละธนาคาร
- แจ้งเตือนเมื่อใกล้พ้น lock-in และควรเริ่มหาข้อเสนอรีไฟแนนซ์
- รายงานสรุปสำหรับยื่นภาษีแบบจัดหน้าพร้อมพิมพ์ (ตัว export ข้อมูลดิบย้ายไป v1 แล้ว ดูข้อ 5A.5)

---

## 13. Prompt สำหรับเปิด Claude Code

```
อ่าน spec ที่ home-loan-app-spec.md ทั้งไฟล์ก่อน แล้วเริ่มที่ M1 เท่านั้น

ข้อบังคับ:
- engine เป็น pure TypeScript ใน src/engine/ ไม่ import React
- เงินเป็น integer สตางค์ อัตราดอกเบี้ยเป็น bps ห้ามใช้ float กับเงิน
- ดอกเบี้ยคิดรายวันจาก balance × rate / ตัวหารตาม day-count basis × วันจริง ห้ามใช้ /12
- day-count มี 3 โหมด (ACT/365F, ACT/ACT, ACT/365_SKIP) เป็น config ห้าม hardcode
  โหมด ACT/ACT ต้องตัดช่วง accrual ที่ขอบปีปฏิทิน
- day-count กับ rounding ต้องเป็น LoanConvention[] แบบ effective-dated ห้ามเป็นค่าเดียวต่อสัญญา
- Net Position ห้ามบวกค่าธรรมเนียม/เบี้ยที่ is_financed = true ซ้ำ (อยู่ในเงินต้นแล้ว) ดูข้อ 2.2
- วันที่ในเอนจินเป็น civil date ล้วน ห้ามใช้ Date ของ JS ที่มี timezone ดูข้อ 8
- ปีใน UI เป็น พ.ศ. แต่ใน DB/engine/test เป็น ISO ค.ศ. เสมอ แปลงผ่าน helper ตัวเดียว
- เบิกเงินกู้หลายงวดอยู่นอกขอบเขต v1 ถ้าเจอเคสนี้ให้หยุดถาม ห้ามแอบใส่
- ห้ามเฉลี่ยอัตราดอกเบี้ยแบบเลขคณิต ต้องถ่วงน้ำหนักด้วยเงินต้นและจำนวนวันเสมอ
- การจัดกลุ่มรายปีทั้งสองแกนต้องมาจากตารางรายงวดชุดเดียวกัน ห้ามคำนวณแยกสองรอบ
- แผนโปะเก็บเป็น 12 เดือน + override รายปี ห้ามทำเป็น array 360 ช่อง
- ห้ามครอบ max-width แบบมือถือไว้ที่ layout ระดับบนสุด desktop ต้องเปลี่ยนโครงสร้าง
  ไม่ใช่แค่ขยายขนาด ดูข้อ 5A
- ช่องกรอกทุกช่อง font-size ขั้นต่ำ 16px และใช้ inputMode="decimal"
- ดอกเบี้ยค้างชำระทำทั้ง 2 โหมด default ไม่ทบต้น (capitalise_unpaid_interest = false)
- ปัดเศษครั้งเดียวตอนจบงวด สะสมระหว่างทางเป็น exact ห้ามปัดรายวัน
- date_roll มี 3 โหมด none/preceding/following + roll_calendar 2 แบบ
  preceding = ขยับมา "เร็วขึ้น" มีอยู่จริงในสัญญาไทย ระวังทิศทางอย่าสลับ
- วันตัดของงวด n ต้อง resolve 3 ชั้น override ?? roll(nominal) ?? nominal
  nominal นับจากวันเริ่มสัญญาเสมอ ห้ามนับต่อจากงวดก่อนหน้า ไม่งั้นวันตัดจะไหล (TV-32)
- first_accrual_date เป็น field ที่ผู้ใช้แก้ได้ ห้าม derive ตายตัวจาก contract_date
- inference ทำ 2 ขั้น หาวันตัดจากคอลัมน์วันที่ก่อน แล้วค่อย fit จำนวนเงิน ห้ามยำรวมกัน
- เพดานลดหย่อนภาษี 100,000 เป็นของ "คน" รวมทุกสัญญา ห้ามทำมิเตอร์แยกรายสัญญา
- เขียน test ตาม TV-1..TV-33 ก่อน แล้วค่อยเขียน implementation
- ทุกครั้งที่ต้องสมมติอะไรที่ spec ไม่ได้ระบุ ให้หยุดถามก่อน อย่าเดา

รายงานกลับเมื่อ test ผ่านครบ พร้อมบอกว่าข้อไหนต้องปรับ spec
```
